# 03. 실습 — Nginx 로드밸런싱과 장애 우회

4주차의 Nginx 앞단 구조를 그대로 유지한 채, 뒤쪽 앱을 `app1`·`app2` 두 개로 늘린다. 이 문서는 기존 파일을 통째로 덮어쓰지 않고, **바꾸거나 추가할 부분만** 보여 준다.

`01-환경세팅.md`를 먼저 끝낸 상태에서 시작한다. 특히 다음 두 가지가 되어 있어야 한다.

- `docker-compose.yml`과 `nginx/conf.d/default.conf`를 `*.before-week5-*`로 백업했다.
- `logs/tomcat/app1/access`, `logs/tomcat/app2/access` 폴더를 만들었다.

## 이 문서를 보는 방법

실습에는 다음 화면을 사용한다. 코드 블록 위의 **입력·수정/실행 위치**를 먼저 확인한다.

| 표시 | 실제로 작업할 곳 | 하는 일 |
| --- | --- | --- |
| 터미널 A — 로그 관찰 | **PetClinic 폴더**로 이동한 첫 번째 터미널 | `docker compose logs -f`를 계속 띄워 로그 관찰 |
| 터미널 B — 명령 실행 | 같은 PetClinic 폴더를 연 두 번째 터미널 | `docker`, `curl`, `grep` 같은 명령 입력 |
| nano 편집 화면 | 터미널 B에서 `nano 파일경로`를 실행하면 열리는 화면 | YAML·Nginx 설정 수정 |
| PC 브라우저 | Chrome·Edge·Safari 등 | `http://localhost:8080` 화면 확인 |

Windows 사용자는 WSL2 Ubuntu 터미널을, macOS 사용자는 기본 터미널을 쓴다. 두 터미널 모두 **4주차에서 사용한 PetClinic 폴더**로 이동한다. 이후 별도 안내가 없는 `bash` 명령은 터미널 B에 입력한다.

```bash
# 현재 위치가 PetClinic 폴더인지 파일 목록으로 확인한다.
ls
```

정상이면 `Dockerfile`, `docker-compose.yml`, `nginx`, `logs` 등이 보인다.

파일을 수정할 때는 터미널 B에서 안내된 `nano 파일경로`를 실행한다. nano 화면에서 내용을 수정한 뒤 다음 순서로 저장하고 나온다.

```text
Ctrl+O → Enter : 저장
Ctrl+X         : nano 종료
```

> **화면 표시 원칙**
> 각 단계는 `검사 목표 → 입력·수정 → 명령 한눈에 보기 → 결과·로그 판정 → 짧은 설명` 순서로 읽는다. 코드 블록의 주석은 **명령 전체의 목적**을 설명하고, 아래 표는 처음 보면 해석하기 어려운 핵심 구성만 설명한다. `curl -sS`처럼 4주차에서 이미 설명한 옵션은 반복하지 않는다.

### 4주차와 달라지는 작업 습관 두 가지

5주차에는 4주차와 다르게 해야 하는 것이 두 가지 있다. 미리 알아 두지 않으면 반드시 막힌다.

| 항목 | 4주차 | 5주차 | 이유 |
| --- | --- | --- | --- |
| Nginx 설정 문법 검사 | 아무 때나 임시 컨테이너로 `nginx -t` | **앱을 먼저 띄운 뒤** 검사 | `upstream`의 `app1`·`app2` 이름을 IP로 바꿀 수 있어야 검사가 통과한다 |
| `nginx -s reload` 시점 | 아무 때나 | **앱이 실행 중일 때만** | 앱이 멈춰 있으면 `host not found in upstream "app1"`으로 실패한다 |

`02-이론.md` 5-3에서 설명한 내용이다. Nginx는 **설정을 읽는 시점에** 이름을 IP로 한 번 변환하고 그 값을 들고 있기 때문이다.

## 목차

- 시작 전. 개념·이론의 구성 요소를 실습 서비스에 대응하기
- 0. 실습 공통: 어느 앱이 처리했는지 판정하는 방법
- 1. `app` 서비스를 `app1`·`app2`로 나누기
- 2. Nginx에 `upstream`과 장애 우회 설정 넣기
- 3. 네 서비스 실행과 공개 포트 확인
- 4. 요청이 두 앱에 분산되는지 확인
- 5. 앱 하나를 중지하고 장애 우회 확인
- 6. 모든 앱을 중지하고 502 확인, 그리고 복구
- 7. (선택) 앱이 느려지는 장애와 타임아웃
- 종료
- 부록. 자주 만나는 오류

---

## 시작 전. 개념·이론의 구성 요소를 실습 서비스에 대응하기

이론에서 구분한 개념은 Compose와 Nginx 설정에서 다음과 같이 나타난다.

| 이론의 개념 | 실습에서 해당하는 것 |
| --- | --- |
| 수평 확장 | `app` 서비스 하나를 `app1`·`app2` 두 개로 나누는 것 |
| 로드밸런서 | `nginx` 서비스 |
| `upstream` | `default.conf` 최상단의 `upstream petclinic_backend` 블록 |
| 라운드 로빈 | `upstream`에 분산 방식을 지정하지 않은 기본 동작 |
| 앱 단위 장애 처리 | `server` 줄의 `max_fails`·`fail_timeout` |
| 요청 단위 장애 처리 | `location` 안의 `proxy_next_upstream`·`proxy_next_upstream_tries` |
| 무상태 | 두 앱이 `postgres:5432` 하나만 바라보는 구성 |
| 판정 근거 | `curl` 상태 코드 + Nginx access·error log + 앱별 Tomcat access log |

이론 마지막의 "이 구조가 아직 해결하지 못한 것"(Nginx·DB 이중화, 롤링 업데이트)은 이번 주차 범위 밖이므로 실습하지 않는다.

---

## 0. 실습 공통: 어느 앱이 처리했는지 판정하는 방법

> **이 단계에서 검사하는 것:** 이번 주차 내내 사용할 로그 확인 명령의 사용법을 먼저 익힌다.

이번 실습의 거의 모든 판정은 **"이 요청이 어느 앱까지 갔는가"** 로 이루어진다. 확인할 근거는 세 가지다.

| 근거 | 확인 대상 | 알 수 있는 것 |
| --- | --- | --- |
| `curl`의 상태 코드 | 사용자가 받은 결과 | 서비스가 살아 있는지 |
| Nginx access log의 `upstream` | Nginx가 실제로 보낸 주소 | 어느 앱에 전달했는지, 재시도했는지 |
| 앱별 Tomcat access log | 각 앱이 받은 요청 | 그 앱까지 실제로 도달했는지 |

**입력·실행 — 터미널 B · 위치: PetClinic 폴더**

```bash
# 네 서비스의 가장 최근 로그 30줄만 모아 기동·오류 상태를 빠르게 확인한다.
docker compose logs --tail 30 nginx app1 app2 postgres

# 최근 2분 동안 발생한 Nginx 로그만 좁혀서 확인한다.
docker compose logs --since 2m nginx

# app1이 이번 시험 요청을 몇 건 처리했는지 센다.
grep -Rc "study=예시" logs/tomcat/app1

# app2가 같은 시험 요청을 몇 건 처리했는지 센다.
grep -Rc "study=예시" logs/tomcat/app2
```

**입력·실행 — 터미널 A**

```bash
# 새로 생기는 Nginx 로그를 실시간으로 확인한다.
docker compose logs -f nginx
```

**명령 한눈에 보기**

| 핵심 구성 | 이 명령에서 하는 역할 |
| --- | --- |
| `logs --tail 30` | 각 서비스의 마지막 로그 30줄만 본다. |
| `logs --since 2m nginx` | 최근 2분의 Nginx 로그만 본다. |
| `grep -R` | 폴더 안의 파일을 모두 뒤진다. Tomcat 로그는 날짜별로 파일이 나뉘므로 필요하다. |
| `grep -c` | 조건에 맞는 **줄의 개수**만 출력한다. 몇 건 처리했는지 셀 때 쓴다. |
| `Ctrl+C` | 로그 보기만 끝내며 컨테이너는 중지하지 않는다. |

**결과·로그 판정**

컨테이너를 아직 실행하지 않았으므로 지금은 출력이 없거나 서비스가 없다는 메시지가 나오는 것이 정상이다. 명령의 모양만 익혀 두고 3단계 이후에 실제로 사용한다.

**짧은 설명**

각 시험 요청에는 `?study=이름` 표식을 붙인다. 이 표식이 Nginx 로그와 Tomcat 로그에 그대로 남기 때문에, 나중에 이번 시험 요청만 골라서 셀 수 있다. 표식이 없으면 브라우저의 부가 요청이나 앞선 실습 기록과 섞여 개수를 셀 수 없다.

---

## 1. `app` 서비스를 `app1`·`app2`로 나누기

### 1-1. 현재 `app` 서비스 확인

> **이 단계에서 검사하는 것:** 수정 대상인 `app` 서비스가 어디에 있고, 볼륨 줄이 어떻게 생겼는지 확인한다.

**입력·실행 — 터미널 B · 위치: PetClinic 폴더**

```bash
# Compose 파일에 정의된 서비스 이름과 줄 번호를 확인한다.
grep -nE '^\s{2}(app|nginx|postgres):' docker-compose.yml

# app 서비스가 Tomcat 로그를 어느 호스트 폴더에 연결하고 있는지 확인한다.
grep -n 'logs/tomcat' docker-compose.yml

# 5주차 백업이 만들어져 있는지 다시 확인한다.
ls -l docker-compose.yml.before-week5-*
```

**결과·로그 판정**

`app:`, `nginx:`, `postgres:` 세 서비스가 보이고, `./logs/tomcat:/tmp/tomcat` 형태의 볼륨 줄이 한 줄 보이면 정상이다. 백업 파일이 없으면 `01-환경세팅.md` 3절을 먼저 실행한다.

**짧은 설명**

앞으로 바꾸는 것은 이 `app` 블록 하나와 `nginx`의 `depends_on` 한 줄뿐이다. `postgres`는 건드리지 않는다.

### 1-2. `app`을 `app1`로 바꾸고 `app2`를 추가

> **이 단계에서 검사하는 것:** 같은 이미지로 앱 컨테이너 두 개를 정의하고, Tomcat 로그 저장 폴더를 앱별로 분리한다.

**입력·수정 — 터미널 B · 위치: PetClinic 폴더**

```bash
# app 서비스를 app1·app2로 나누기 위해 Compose 파일을 연다.
nano docker-compose.yml
```

nano 화면에서 다음 세 가지를 수정한다.

**① `app:`을 `app1:`로 바꾸고 볼륨 경로를 바꾼다**

```yaml
  app1:                                    # app → app1 로 이름만 변경
    # image, environment, depends_on, networks 등 나머지 줄은 4주차 내용 그대로 둔다
    volumes:
      - ./logs/tomcat/app1:/tmp/tomcat     # ./logs/tomcat → ./logs/tomcat/app1
```

**② `app1:` 블록 전체를 복사해 바로 아래에 `app2:`로 붙여 넣는다**

```yaml
  app2:                                    # 복사한 블록의 이름만 app2로
    # 나머지 줄은 app1과 완전히 동일하게 둔다
    volumes:
      - ./logs/tomcat/app2:/tmp/tomcat     # 이 한 줄만 app2로 다르게
```

**③ `nginx` 서비스의 `depends_on`을 바꾼다**

```yaml
  nginx:
    depends_on:
      - app1                               # 기존 app 한 줄을 app1·app2 두 줄로
      - app2
```

> **주의 1:** `app1`과 `app2`의 차이는 **서비스 이름과 볼륨 한 줄뿐**이다. `image`, `environment`, `expose`, `depends_on`, `networks`는 완전히 같아야 한다. 특히 `SPRING_SQL_INIT_MODE: never`가 **양쪽 모두에** 있어야 한다.
>
> **주의 2:** 4주차 `app` 블록에 `container_name:`이 있었다면 두 서비스가 같은 이름을 쓸 수 없다. `container_name`을 각각 다르게 바꾸거나, 두 줄 모두 지운다. 지우면 Compose가 `프로젝트명-app1-1` 형태로 자동 생성한다.
>
> **주의 3:** `app1`·`app2` 어느 쪽에도 `ports:`를 넣지 않는다. `expose: ["8080"]`만 둔다.

**입력·실행 — 터미널 B**

```bash
# 합쳐진 최종 Compose 설정에 문법 오류가 없는지 확인한다.
docker compose config >/dev/null && echo "Compose 문법 정상"

# app1·app2·nginx·postgres 네 서비스가 정의됐는지 확인한다.
docker compose config --services

# 두 앱의 볼륨 경로가 서로 다르게 지정됐는지 확인한다.
grep -n 'logs/tomcat' docker-compose.yml

# 두 앱 어디에도 호스트 포트가 열려 있지 않은지 확인한다.
docker compose config | grep -A2 -E '^\s{2}app[12]:' | grep -c 'published' || echo "호스트 포트 없음"
```

**명령 한눈에 보기**

| 핵심 구성 | 이 명령에서 하는 역할 |
| --- | --- |
| `docker compose config` | 여러 Compose 파일을 합친 **최종 결과**를 만들어 문법을 검사한다. |
| `--services` | 정의된 서비스 이름만 목록으로 출력한다. |
| `>/dev/null && echo ...` | 긴 출력을 감추고 성공했을 때만 짧은 메시지를 남긴다. |

**결과·로그 판정**

- `Compose 문법 정상`이 출력된다.
- `--services` 결과에 `app1`, `app2`, `nginx`, `postgres` 네 개가 모두 보인다.
- `logs/tomcat` 검색 결과가 `./logs/tomcat/app1`, `./logs/tomcat/app2` 두 줄이다.
- 마지막 명령에서 `0` 또는 `호스트 포트 없음`이 출력된다.

문법 오류가 나면 대부분 YAML 들여쓰기 문제다. `app2:`의 들여쓰기가 `app1:`과 정확히 같은지 확인한다.

**짧은 설명**

컨테이너 안의 경로는 `app1`·`app2` 모두 `/tmp/tomcat`으로 같고 **호스트 쪽 폴더만 다르다.** 컨테이너는 자기 밖을 모르므로 두 앱의 환경 변수가 완전히 같아도 로그는 다른 곳에 쌓인다. 이것이 환경 변수를 건드리지 않아도 되는 이유다.

`docker compose up --scale app=2`를 쓰면 컨테이너는 두 개가 되지만 **볼륨을 공유**해 두 앱의 로그가 한 파일에 섞인다. 이번 주차의 목적은 "어느 앱이 처리했는가"를 눈으로 확인하는 것이므로 서비스를 명시적으로 나눈다.

---

## 2. Nginx에 `upstream`과 장애 우회 설정 넣기

### 2-1. 현재 `proxy_pass` 위치 확인

> **이 단계에서 검사하는 것:** 바꿔야 할 `proxy_pass`가 정확히 몇 군데인지 먼저 센다.

**입력·실행 — 터미널 B · 위치: PetClinic 폴더**

```bash
# 4주차 설정에 proxy_pass가 몇 줄 있고 어느 줄인지 확인한다.
grep -n 'proxy_pass' nginx/conf.d/default.conf

# location 블록의 위치도 함께 확인해 어느 proxy_pass가 어디 속하는지 파악한다.
grep -n -E 'location|proxy_pass' nginx/conf.d/default.conf
```

**결과·로그 판정**

`proxy_pass http://app:8080;`이 **두 줄** 나와야 한다. 하나는 정적 파일 `location`, 다른 하나는 일반 `location /` 안에 있다.

**짧은 설명**

`02-이론.md` 3-2의 주의 사항이다. 하나만 바꾸면 나머지 하나가 존재하지 않는 `app`을 가리키게 되고, Nginx는 `host not found in upstream "app"` 오류로 **아예 시작하지 못한다.** 이번 주차에서 가장 흔한 사고이므로 지금 두 줄이라는 것을 눈으로 확인해 둔다.

### 2-2. `upstream` 블록 추가와 `proxy_pass` 변경

> **이 단계에서 검사하는 것:** 앱 서버 묶음에 이름을 붙이고, 두 `proxy_pass`가 모두 그 이름을 가리키게 한다.

**입력·수정 — 터미널 B · 위치: PetClinic 폴더**

```bash
# upstream 블록과 장애 우회 설정을 넣기 위해 Nginx 웹 서버 설정 파일을 연다.
nano nginx/conf.d/default.conf
```

**① 파일 맨 위, `server {` 블록보다 위에 다음을 추가한다**

```nginx
# 파일 최상단에 추가 — server { } 블록 바깥
upstream petclinic_backend {
    server app1:8080 max_fails=1 fail_timeout=10s;
    server app2:8080 max_fails=1 fail_timeout=10s;
}
```

**② 두 `proxy_pass`를 모두 바꾼다**

```nginx
# 변경 전 (두 군데 모두)
proxy_pass http://app:8080;

# 변경 후 (두 군데 모두)
proxy_pass http://petclinic_backend;
```

**③ 두 `location` 안, `proxy_pass` 바로 아래에 장애 우회 설정을 추가한다**

```nginx
# 각 location 안 proxy_pass 아래에 추가
proxy_next_upstream error timeout http_502 http_503 http_504;
proxy_next_upstream_tries 2;
```

**입력·실행 — 터미널 B**

```bash
# upstream 블록이 server 블록 바깥에 추가됐는지 줄 번호로 확인한다.
grep -n -E 'upstream petclinic_backend|^server|server app[12]:8080' nginx/conf.d/default.conf

# 새 전달 대상이 두 군데 모두 바뀌었는지 개수로 확인한다.
grep -c 'proxy_pass http://petclinic_backend;' nginx/conf.d/default.conf

# 옛 전달 대상이 하나도 남지 않았는지 확인한다.
grep -c 'app:8080' nginx/conf.d/default.conf || echo "app:8080 없음"

# 장애 우회 설정이 두 location에 모두 들어갔는지 확인한다.
grep -c 'proxy_next_upstream ' nginx/conf.d/default.conf
```

**명령 한눈에 보기**

| 핵심 구성 | 이 명령에서 하는 역할 |
| --- | --- |
| `grep -n '^server'` | 줄 맨 앞의 `server`만 찾아 `server {` 블록 시작 위치를 본다. `upstream` 안의 `server app1:8080`은 들여쓰기가 있어 걸리지 않는다. |
| `grep -c` | 개수만 센다. 2가 나와야 하는 항목과 0이 나와야 하는 항목을 구분해 확인한다. |
| `\|\| echo ...` | 앞 명령이 실패했을 때(=한 건도 못 찾았을 때) 뒤 메시지를 출력한다. |

**결과·로그 판정**

| 확인 항목 | 정상 값 |
| --- | --- |
| `upstream petclinic_backend` 줄 번호 | `server {` 줄 번호보다 **작아야** 한다 |
| `proxy_pass http://petclinic_backend;` 개수 | `2` |
| `app:8080` 개수 | `0` 또는 `app:8080 없음` |
| `proxy_next_upstream ` 개수 | `2` |

`proxy_pass` 개수가 1이면 한 군데를 빠뜨린 것이다. 다시 열어 나머지 하나를 찾는다.

**짧은 설명**

`petclinic_backend`는 호스트 이름이 아니라 **우리가 붙인 별명**이다. 목록에 이름을 붙였기 때문에, 나중에 앱을 세 대·네 대로 늘려도 `proxy_pass`는 고치지 않고 `upstream` 안의 목록만 늘리면 된다.

`upstream`은 `http` 범위에 써야 한다. `conf.d/*.conf`의 내용은 Nginx 기본 설정의 `http {}` 안에 포함되므로, `server {}` **바깥**, 파일 맨 위에 두면 된다. `server {}` 안에 넣으면 문법 오류가 난다.

`max_fails=1`, `fail_timeout=10s`는 실습에서 결과를 빨리 보기 위한 값이다. 실제 운영에서는 일시적인 오류로 앱이 너무 쉽게 제외되지 않도록 더 큰 값을 쓴다.

> **참고:** `upstream`에 서버가 한 대뿐이면 Nginx는 `max_fails`·`fail_timeout`을 무시하고 그 서버를 절대 제외하지 않는다. 제외해 봐야 보낼 곳이 없기 때문이다. 그래서 4주차 구조에서는 이 설정이 의미가 없었고, 앱이 두 대가 된 지금부터 처음 의미를 갖는다.

**아직 `nginx -t`를 실행하지 않는다.** 지금 검사하면 `app1`이라는 이름을 IP로 바꿀 수 없어 실패한다. 3단계에서 앱을 먼저 띄운 뒤 검사한다.

---

## 3. 네 서비스 실행과 공개 포트 확인

### 3-1. 앱을 먼저 띄우고 Nginx 설정을 검사

> **이 단계에서 검사하는 것:** `upstream`의 이름이 실제로 해석되는 상태에서 Nginx 설정 문법을 검사한다.

**입력·실행 — 터미널 B · 위치: PetClinic 폴더**

```bash
# DB와 두 PetClinic 앱을 먼저 실행한다. 4주차의 app 컨테이너가 남아 있으면 함께 정리한다.
docker compose up -d --remove-orphans postgres app1 app2

# 두 앱이 완전히 기동되어 Started 문구가 나오는지 확인한다.
docker compose logs --tail 5 app1 app2 | grep -E 'Started|ERROR' || echo "아직 기동 중"

# Nginx가 사용할 네트워크 이름을 찾는다.
NET=$(docker network ls --format '{{.Name}}' | grep 'proxy-net' | head -1)
echo "사용할 네트워크: $NET"

# 앱 이름을 해석할 수 있는 네트워크에 붙여 임시 컨테이너로 Nginx 설정을 검사한다.
docker run --rm --network "$NET" \
  -v "$PWD/nginx/conf.d:/etc/nginx/conf.d:ro" \
  nginx:stable-alpine nginx -t
```

**명령 한눈에 보기**

| 핵심 구성 | 이 명령에서 하는 역할 |
| --- | --- |
| `--remove-orphans` | Compose 파일에서 사라진 4주차 `app` 컨테이너를 함께 정리한다. |
| `docker network ls \| grep proxy-net` | 프로젝트 이름이 앞에 붙은 실제 네트워크 이름을 찾는다. |
| `docker run --rm --network "$NET"` | **앱 이름을 해석할 수 있는 네트워크에** 임시 컨테이너를 붙여 실행하고, 끝나면 지운다. |
| `-v ...:ro` | 호스트의 설정 폴더를 읽기 전용으로 연결한다. |

**결과·로그 판정**

```text
nginx: configuration file /etc/nginx/nginx.conf test is successful
```

이 문구가 나오면 정상이다. `host not found in upstream "app1"`이 나오면 두 가지 중 하나다.

| 원인 | 확인 방법 |
| --- | --- |
| 앱이 아직 안 떴다 | `docker compose ps`로 `app1`·`app2` 상태 확인 |
| 네트워크를 잘못 붙였다 | `echo $NET` 결과가 비어 있지 않은지 확인 |

**짧은 설명**

4주차에는 임시 컨테이너에 네트워크를 붙이지 않고도 `nginx -t`가 통과했다. `proxy_pass http://app:8080`은 요청이 들어올 때 해석해도 되기 때문이다. 그런데 **`upstream` 안의 이름은 설정을 읽는 시점에 반드시 해석돼야 한다.** 그래서 5주차부터는 검사에도 네트워크가 필요하다.

이것이 "앱이 멈춘 상태에서는 `nginx -s reload`를 하지 않는다"는 규칙의 근거이기도 하다. 같은 이유에서 나오는 제약이다.

### 3-2. Nginx 실행과 공개 포트 확인

> **이 단계에서 검사하는 것:** 네 서비스가 모두 실행되고, 호스트에 공개된 포트가 Nginx의 8080 하나뿐인지 확인한다.

**입력·실행 — 터미널 B**

```bash
# 문법 검사가 통과한 설정으로 Nginx까지 포함해 전체를 실행한다.
docker compose up -d

# 네 서비스의 실행 상태와 호스트 공개 포트를 확인한다.
docker compose ps

# Nginx를 통해 PetClinic이 200으로 열리는지 확인한다.
curl -s -o /dev/null -w "%{http_code}\n" "http://localhost:8080/?study=3-2-first"

# 방금 보낸 요청이 Nginx 로그에 어떤 형식으로 남았는지 확인한다.
docker compose logs --since 2m nginx | grep 'study=3-2-first'
```

**결과·로그 판정**

`docker compose ps`의 PORTS 열이 다음과 같아야 한다.

```text
nginx     → 0.0.0.0:8080->80/tcp     ← 호스트에 공개된 유일한 포트
app1      → 8080/tcp                 ← 내부 전용
app2      → 8080/tcp                 ← 내부 전용
postgres  → 5432/tcp                 ← 내부 전용
```

`app1`이나 `app2` 옆에 `0.0.0.0:...->8080/tcp`가 보이면 `ports:`가 들어간 것이다. 1-2로 돌아가 지운다.

`curl` 결과는 `200`이고, Nginx 로그에 `status=200`과 `upstream="172.x.x.x:8080"` 형태의 줄이 남는다.

**짧은 설명**

앱에 `ports:`를 붙이면 두 가지 문제가 생긴다. 첫째, 두 앱이 같은 호스트 포트를 쓸 수 없어 서로 다른 포트를 줘야 한다. 둘째, 사용자가 **로드밸런서를 건너뛰고** 특정 앱에 직접 접속할 수 있게 되어 구조가 무너진다. 그 앱이 죽으면 그 사용자는 우회 없이 그대로 실패한다.

### 3-3. 컨테이너 IP 대응표 만들기

> **이 단계에서 검사하는 것:** Nginx 로그의 `upstream` IP가 어느 앱인지 알아볼 수 있게 대응표를 만든다.

**입력·실행 — 터미널 B**

```bash
# app1·app2가 속한 네트워크별 IP를 각각 조회한다.
for s in app1 app2; do
  cid=$(docker compose ps -q $s)
  echo -n "$s : "
  docker inspect -f '{{range $k, $v := .NetworkSettings.Networks}}{{$k}}={{$v.IPAddress}} {{end}}' "$cid"
done
```

**명령 한눈에 보기**

| 핵심 구성 | 이 명령에서 하는 역할 |
| --- | --- |
| `docker compose ps -q app1` | `app1` 컨테이너의 ID만 출력한다. |
| `docker inspect -f '{{range ...}}'` | 컨테이너 정보 중 **네트워크 이름과 IP만** 골라 출력한다. |
| `for s in app1 app2; do ... done` | 두 앱에 같은 조회를 반복한다. |

**결과·로그 판정**

각 앱마다 **IP가 두 개** 나온다. `proxy-net`과 `db-net` 두 네트워크에 속해 있기 때문이다.

```text
app1 : petclinic_db-net=172.19.0.3 petclinic_proxy-net=172.18.0.4
app2 : petclinic_db-net=172.19.0.4 petclinic_proxy-net=172.18.0.5
```

**Nginx 로그에 찍히는 것은 `proxy-net` 쪽 IP**다. Nginx는 `proxy-net`을 통해 앱에 연결하기 때문이다. 위 예시라면 다음 대응표를 손에 적어 둔다.

```text
172.18.0.4 = app1
172.18.0.5 = app2
```

**짧은 설명**

4주차 로그 형식에는 이미 `upstream="$upstream_addr"`이 들어 있다. 그런데 이 값은 `app1`이 아니라 **IP 주소로 기록된다.** Nginx가 설정을 읽는 시점에 이름을 IP로 바꿔 두기 때문이다. 그래서 로그만 봐서는 어느 앱인지 알 수 없고 이 대응표가 필요하다.

> **주의:** 컨테이너를 **재생성**하면 IP가 바뀔 수 있다. `stop`/`start`는 대개 유지되지만, `docker compose up --force-recreate`를 하면 이 표를 다시 만들어야 한다.

---

## 4. 요청이 두 앱에 분산되는지 확인

### 4-1. 요청 10건을 보내고 앱별 처리 건수 세기

> **이 단계에서 검사하는 것:** Nginx가 라운드 로빈으로 요청을 두 앱에 나누어 전달하는지 확인한다.

**입력·실행 — 터미널 B · 위치: PetClinic 폴더**

```bash
# 캐시되지 않는 동적 경로로 요청 10건을 순서대로 보낸다.
for i in $(seq 1 10); do
  curl -s -o /dev/null -w "%{http_code}\n" \
    "http://localhost:8080/owners?study=week5-$i"
done

# app1이 이번 10건 중 몇 건을 처리했는지 센다.
grep -Rc "study=week5-" logs/tomcat/app1

# app2가 이번 10건 중 몇 건을 처리했는지 센다.
grep -Rc "study=week5-" logs/tomcat/app2

# Nginx 로그에서 각 요청이 어느 upstream으로 갔는지 확인한다.
docker compose logs --since 5m nginx | grep 'study=week5-'
```

**명령 한눈에 보기**

| 핵심 구성 | 이 명령에서 하는 역할 |
| --- | --- |
| `for i in $(seq 1 10)` | 1부터 10까지 반복하며 요청마다 다른 번호를 붙인다. |
| `?study=week5-$i` | 요청마다 고유 표식을 붙여 나중에 로그에서 골라낸다. |
| `grep -Rc` | 앱별 로그 폴더 전체에서 표식이 있는 줄 수를 센다. |

**결과·로그 판정**

| 확인 항목 | 정상 값 |
| --- | --- |
| `curl` 상태 코드 10줄 | 모두 `200` |
| app1 처리 건수 + app2 처리 건수 | 합이 `10` |
| 각 앱의 건수 | 대체로 `5` / `5` |
| Nginx 로그의 `upstream` | 3-3의 두 IP가 번갈아 나타남 |

정확히 5:5가 아닐 수 있다. 이유는 네 가지다.

| 원인 | 결과 |
| --- | --- |
| 앞선 실습에서 이미 요청이 나갔다 | 시작 차례가 app1이 아닐 수 있다 |
| 앱 하나가 실패로 제외된 직후다 | 제외 기간에는 한쪽으로만 간다 |
| 정적 파일 요청을 섞었다 | 캐시 `HIT`이면 앱까지 가지 않는다 |
| 브라우저로 확인했다 | CSS·이미지 부가 요청이 섞여 들어온다 |

합이 10이고 양쪽 모두 0이 아니면 분산은 성공이다.

**짧은 설명**

`/owners`를 쓰는 이유가 여기 있다. 4주차의 정적 파일 캐시 설정은 그대로 유지하지만, 이미지 요청은 Nginx 캐시에서 끝나 앱까지 가지 않으므로 **로드밸런싱 확인에는 쓸 수 없다.** 캐시하지 않는 동적 경로여야 앱까지 도달한 기록이 남는다.

브라우저가 아니라 `curl`을 쓰는 이유도 같다. 브라우저는 화면 하나를 여는 데 CSS·이미지 요청을 여러 건 함께 보내므로 개수를 셀 수 없다.

### 4-2. 캐시된 요청은 분산 확인에 쓸 수 없음을 직접 보기

> **이 단계에서 검사하는 것:** 정적 파일 요청이 왜 분산 확인에 부적합한지 로그로 확인한다.

**입력·실행 — 터미널 B**

```bash
# 4주차 캐시 시험에 쓴 정적 이미지 경로를 다시 사용한다.
IMG="http://localhost:8080/resources/images/pets.png"

# 같은 이미지를 5번 요청한다.
for i in $(seq 1 5); do
  curl -s -o /dev/null -w "%{http_code}\n" "$IMG?study=week5-static-$i"
done

# Nginx 로그에서 캐시 처리 결과를 확인한다.
docker compose logs --since 2m nginx | grep 'study=week5-static-'

# 두 앱의 로그에 이 요청이 남았는지 확인한다.
grep -Rc "study=week5-static-" logs/tomcat/app1
grep -Rc "study=week5-static-" logs/tomcat/app2
```

**결과·로그 판정**

- Nginx 로그의 `cache` 항목이 첫 요청은 `MISS`, 이후는 `HIT`이다.
- 두 앱의 로그 건수 합이 **5보다 작다.** `HIT`인 요청은 앱까지 가지 않았기 때문이다.

**짧은 설명**

`cache=HIT`인 요청은 Nginx가 직접 응답했으므로 **로드밸런싱과 무관하다.** 판정 조합표로 정리하면 다음과 같다.

| Nginx 로그 | app1 로그 | app2 로그 | 해석 |
| --- | --- | --- | --- |
| `status=200` | 있음 | 없음 | app1이 처리 |
| `status=200` | 없음 | 있음 | app2가 처리 |
| `status=200`, `upstream`에 주소 2개 | 없음 | 있음 | app1 실패 → app2로 우회 성공 |
| `status=502` | 없음 | 없음 | 전달할 앱이 없음 |
| `cache=HIT` | 없음 | 없음 | Nginx 캐시가 응답. 로드밸런싱과 무관 |

세 번째 줄은 5단계에서, 네 번째 줄은 6단계에서 직접 만들어 본다.

### 4-3. 두 앱이 같은 데이터를 보는지 확인

> **이 단계에서 검사하는 것:** 한쪽 앱을 통해 저장한 데이터를 다른 앱이 처리한 요청에서도 조회할 수 있는지 확인한다.

**입력·실행 — 터미널 B**

```bash
# 새 소유자를 한 명 등록하고, 생성된 소유자 주소를 받아 온다.
REDIRECT=$(curl -s -o /dev/null -w "%{redirect_url}" \
  -X POST "http://localhost:8080/owners/new?study=week5-write" \
  -d "firstName=Week5" -d "lastName=Sharing" \
  -d "address=Sogang" -d "city=Seoul" -d "telephone=01012345678")

# 주소에서 소유자 번호만 잘라 낸다.
OWNER_ID=$(echo "$REDIRECT" | sed 's#.*/owners/##')
echo "등록된 소유자 번호: $OWNER_ID"

# 같은 소유자 상세 페이지를 6번 조회한다.
for i in $(seq 1 6); do
  curl -s "http://localhost:8080/owners/$OWNER_ID?study=week5-read-$i" \
    | grep -c "Sharing"
done

# 이 조회 요청을 두 앱이 나누어 처리했는지 확인한다.
grep -Rc "study=week5-read-" logs/tomcat/app1
grep -Rc "study=week5-read-" logs/tomcat/app2
```

**명령 한눈에 보기**

| 핵심 구성 | 이 명령에서 하는 역할 |
| --- | --- |
| `-w "%{redirect_url}"` | 등록 성공 후 Nginx가 알려 준 **이동할 주소**만 출력한다. |
| `-X POST -d "키=값"` | 폼 데이터를 담아 POST 요청을 보낸다. |
| `sed 's#.*/owners/##'` | 주소에서 `/owners/` 앞부분을 지워 번호만 남긴다. |
| `grep -c "Sharing"` | 응답 화면에 등록한 이름이 들어 있는지 개수로 확인한다. |

**결과·로그 판정**

- `등록된 소유자 번호`에 숫자가 출력된다.
- 조회 6번 모두 `1` 이상이 출력된다. 즉 **어느 앱이 처리했든 같은 데이터가 보인다.**
- 두 앱의 로그 건수 합이 `6`이고 양쪽 모두 0이 아니다.

`REDIRECT`가 비어 있으면 브라우저에서 직접 등록해도 된다. `http://localhost:8080/owners/new`에서 등록한 뒤 주소창의 번호를 `OWNER_ID`에 넣으면 된다.

**짧은 설명**

이것이 **무상태(stateless)** 구조가 성립한다는 증거다. 데이터는 앱이 아니라 `postgres:5432` 한 곳에 있으므로, 요청을 어느 앱이 처리하든 결과가 같다.

만약 데이터를 앱이 자기 안에 갖고 있었다면 등록은 app1에서 되고 조회는 app2에서 실패했을 것이다. PetClinic은 로그인 기능이 없어 세션 문제도 드러나지 않는다. 로그인이 있는 서비스라면 세션을 DB나 Redis 같은 공용 저장소로 옮기거나 `ip_hash`로 같은 서버에 묶어야 한다.

---

## 5. 앱 하나를 중지하고 장애 우회 확인

### 5-1. `app1`을 중지하고 서비스가 유지되는지 확인

> **이 단계에서 검사하는 것:** 앱 하나가 멈춰도 다른 앱이 요청을 처리해 `200 OK`가 유지되는지 확인한다.

**입력·실행 — 터미널 A**

```bash
# 우회가 일어나는 순간을 실시간으로 보기 위해 Nginx 로그를 띄워 둔다.
docker compose logs -f nginx
```

**입력·실행 — 터미널 B · 위치: PetClinic 폴더**

```bash
# 장애 상황을 만들기 위해 app1만 중지한다.
docker compose stop app1

# app1이 중지되고 app2·nginx·postgres는 실행 중인지 확인한다.
docker compose ps

# app1이 멈춘 상태에서 요청을 보내 최종 상태 코드와 응답 시간을 확인한다.
curl -s -o /dev/null -w "status=%{http_code} time=%{time_total}s\n" \
  "http://localhost:8080/owners?study=app1-down"

# 같은 요청을 4건 더 보내 두 번째 이후에는 빨라지는지 확인한다.
for i in $(seq 2 5); do
  curl -s -o /dev/null -w "status=%{http_code} time=%{time_total}s\n" \
    "http://localhost:8080/owners?study=app1-down-$i"
done
```

**명령 한눈에 보기**

| 핵심 구성 | 이 명령에서 하는 역할 |
| --- | --- |
| `docker compose stop app1` | 컨테이너를 **중지만** 한다. 삭제하지 않으므로 `start`로 되살릴 수 있다. |
| `%{time_total}` | 요청 전체에 걸린 시간을 초 단위로 출력한다. |

**결과·로그 판정**

| 확인 항목 | 정상 값 |
| --- | --- |
| 첫 요청 상태 | `200` |
| 이후 4건 상태 | 모두 `200` |
| 응답 시간 | 첫 요청이 조금 더 길고, 이후는 짧아진다 |

사용자에게는 아무 문제가 없어 보인다. 진짜 확인은 로그에서 한다.

### 5-2. 우회가 실제로 일어났는지 로그로 판정

> **이 단계에서 검사하는 것:** 사용자에게 200이 나갔지만 내부에서는 실패와 우회가 있었다는 것을 로그로 확인한다.

**입력·실행 — 터미널 B**

```bash
# Nginx error log에서 app1 연결 실패 기록을 찾는다.
docker compose logs --since 5m nginx | grep -E 'connect\(\) failed|no live upstreams'

# Nginx access log에서 이번 요청의 최종 상태와 실제 전달 주소를 확인한다.
docker compose logs --since 5m nginx | grep 'study=app1-down'

# app2 Tomcat 로그에 요청이 도달했는지 확인한다.
grep -Rc "study=app1-down" logs/tomcat/app2

# app1 Tomcat 로그에는 기록이 없는지 확인한다.
grep -Rc "study=app1-down" logs/tomcat/app1
```

**결과·로그 판정**

| 로그 | 정상 값 | 뜻 |
| --- | --- | --- |
| error log | `connect() failed (111: Connection refused)` | app1 연결이 거부됐다 |
| access log 첫 요청 | `status=200`, `upstream="IP1:8080, IP2:8080"` | **한 요청이 두 대상에 순서대로 시도됐다** |
| access log 이후 요청 | `status=200`, `upstream="IP2:8080"` | app1을 아예 시도하지 않았다 |
| app2 로그 건수 | `5` | 살아 있는 앱이 전부 처리했다 |
| app1 로그 건수 | `0` | 멈춘 앱에는 기록이 없다 |

**쉼표로 IP가 두 개 기록된 줄이 이번 실습에서 가장 중요한 증거다.** 이것이 장애 우회가 실제로 동작했다는 가장 직접적인 표시다.

**짧은 설명**

첫 요청과 이후 요청의 차이가 두 설정의 역할 차이를 보여 준다.

```text
첫 요청  : app1 시도 → 실패 → proxy_next_upstream 규칙으로 app2 재시도 → 200
           동시에 max_fails=1 도달 → app1을 10초간 제외 대상으로 표시

이후 요청: app1을 아예 시도하지 않음 → 바로 app2 → 200 (지연 없음)
```

| 설정 | 시간 축 | 하는 일 |
| --- | --- | --- |
| `proxy_next_upstream` | 요청 단위 | 지금 이 요청 하나를 구제한다 |
| `max_fails`·`fail_timeout` | 앱 단위 | 앞으로 이 앱을 10초간 건너뛴다 |

`max_fails`가 없으면 매 요청마다 죽은 앱에 먼저 시도했다가 실패하고 우회하는 일이 반복돼, 사용자는 200을 받지만 **매번 느려진다.**

수동 감지의 한계도 여기서 드러난다. **실패를 알아채기 위해 실제 요청 하나가 희생된다.** 그 요청은 우회 덕분에 성공으로 보이지만 응답 시간이 조금 길어진다. Nginx가 앱 상태를 미리 검사하는 능동 헬스체크는 상용 버전인 NGINX Plus 기능이다.

### 5-3. `app1`을 복구하고 다시 분산되는지 확인

> **이 단계에서 검사하는 것:** 제외됐던 앱이 다시 실행되면 분산 대상으로 돌아오는지 확인한다.

**입력·실행 — 터미널 B**

```bash
# 중지했던 app1을 다시 시작한다.
docker compose start app1

# app1이 완전히 기동될 때까지 기다린 뒤 상태를 확인한다.
sleep 20 && docker compose ps

# 요청 6건을 보내 다시 두 앱에 나뉘는지 확인한다.
for i in $(seq 1 6); do
  curl -s -o /dev/null -w "%{http_code}\n" \
    "http://localhost:8080/owners?study=app1-back-$i"
done

# 두 앱의 처리 건수를 각각 센다.
grep -Rc "study=app1-back-" logs/tomcat/app1
grep -Rc "study=app1-back-" logs/tomcat/app2
```

**결과·로그 판정**

- 상태 코드 6건 모두 `200`
- 두 앱의 건수 합이 `6`이고 양쪽 모두 0이 아니다.

app1 건수가 0이면 아직 기동이 끝나지 않았거나 `fail_timeout` 10초가 지나지 않은 것이다. 잠시 뒤 다시 보낸다.

**짧은 설명**

`fail_timeout=10s`는 **제외를 유지하는 기간**이기도 하다. 10초가 지나면 Nginx는 제외했던 앱에 다시 요청을 보내 본다. 살아났으면 정상 대상으로 돌아오고, 여전히 죽어 있으면 다시 실패로 세고 또 10초 제외한다.

### 5-4. 반대 방향으로도 확인

> **이 단계에서 검사하는 것:** `app2`를 중지했을 때 `app1`이 처리하는지 확인해, 앞의 결과가 우연이 아님을 검증한다.

**입력·실행 — 터미널 B**

```bash
# 이번에는 app2만 중지한다.
docker compose stop app2

# 요청 5건을 보내 상태 코드를 확인한다.
for i in $(seq 1 5); do
  curl -s -o /dev/null -w "%{http_code}\n" \
    "http://localhost:8080/owners?study=app2-down-$i"
done

# 이번에는 app1이 전부 처리했는지 확인한다.
grep -Rc "study=app2-down-" logs/tomcat/app1
grep -Rc "study=app2-down-" logs/tomcat/app2

# app2 연결 실패가 error log에 남았는지 확인한다.
docker compose logs --since 3m nginx | grep 'connect() failed'

# 확인이 끝나면 app2를 다시 시작한다.
docker compose start app2
```

**결과·로그 판정**

| 확인 항목 | 정상 값 |
| --- | --- |
| 상태 코드 5건 | 모두 `200` |
| app1 로그 건수 | `5` |
| app2 로그 건수 | `0` |
| error log | app2 IP에 대한 `connect() failed` |

**짧은 설명**

한쪽 방향만 확인하면 "우연히 app2가 처리하고 있었을 뿐"인 상황과 구분되지 않는다. 양쪽 모두에서 같은 결과가 나와야 **어느 앱이 죽어도 나머지가 받는다**고 말할 수 있다.

---

## 6. 모든 앱을 중지하고 502 확인, 그리고 복구

### 6-1. 두 앱을 모두 중지

> **이 단계에서 검사하는 것:** 전달할 앱이 하나도 없을 때 502가 반환되고, 안내 화면이 나와도 상태 코드는 502로 유지되는지 확인한다.

**입력·실행 — 터미널 B · 위치: PetClinic 폴더**

```bash
# 두 앱을 모두 중지해 전달할 대상이 없는 상태를 만든다.
docker compose stop app1 app2

# nginx와 postgres만 실행 중인지 확인한다.
docker compose ps

# 응답 헤더까지 함께 출력해 최종 상태 코드를 확인한다.
curl -i "http://localhost:8080/owners?study=all-down" | head -20

# 같은 요청을 두 번 더 보내 error log 메시지가 어떻게 바뀌는지 본다.
curl -s -o /dev/null -w "%{http_code}\n" "http://localhost:8080/owners?study=all-down-2"
curl -s -o /dev/null -w "%{http_code}\n" "http://localhost:8080/owners?study=all-down-3"

# Nginx error log의 두 가지 메시지를 확인한다.
docker compose logs --since 3m nginx | grep -E 'connect\(\) failed|no live upstreams'

# 어느 앱 로그에도 기록이 없는지 확인한다.
grep -Rc "study=all-down" logs/tomcat/app1
grep -Rc "study=all-down" logs/tomcat/app2
```

**명령 한눈에 보기**

| 핵심 구성 | 이 명령에서 하는 역할 |
| --- | --- |
| `curl -i` | 응답 **헤더와 본문을 함께** 출력한다. 상태 줄을 눈으로 확인할 때 쓴다. |
| `head -20` | 앞 20줄만 보여 안내 HTML 전체가 쏟아지는 것을 막는다. |

**결과·로그 판정**

응답 첫 줄이 다음과 같아야 한다.

```text
HTTP/1.1 502 Bad Gateway
```

그 아래에 4주차에서 만든 안내 HTML의 내용이 이어진다. **화면은 안내 문구여도 상태 줄은 `502`여야 한다.**

error log에는 두 가지 메시지가 순서대로 나타난다.

| 메시지 | 언제 나오는가 |
| --- | --- |
| `connect() failed (111: Connection refused)` | 앱에 연결을 시도했으나 거부됨 |
| `no live upstreams while connecting to upstream` | 두 앱이 모두 제외 상태여서 시도할 대상 자체가 없음 |

두 앱의 Tomcat 로그 건수는 모두 `0`이다.

**짧은 설명**

`no live upstreams`는 **앱이 두 대 이상일 때만 나오는 표현**이다. 4주차에는 볼 수 없던 메시지다. `max_fails`로 두 앱이 모두 제외 표시된 뒤에 들어온 요청에서 볼 수 있다.

상태 코드를 `200`으로 바꾸면 안 되는 이유가 여기 있다. **화면은 사람에게, 상태 코드는 기계에게 보내는 신호다.** 200으로 바꾸면 모니터링 도구가 장애를 정상으로 집계해 알람이 울리지 않는다. 브라우저 화면만 보고 판정할 수 없는 이유이기도 하다. 반드시 `curl -i`로 상태 줄을 본다.

### 6-2. 앱이 멈춘 상태에서 reload가 실패하는 것 확인

> **이 단계에서 검사하는 것:** 앱이 멈춰 있을 때 Nginx 설정을 다시 읽으면 왜 실패하는지 직접 본다.

**입력·실행 — 터미널 B**

```bash
# 앱이 모두 멈춘 상태에서 Nginx 설정 문법 검사를 시도한다.
docker compose exec nginx nginx -t
```

**결과·로그 판정**

```text
nginx: [emerg] host not found in upstream "app1" in /etc/nginx/conf.d/default.conf:2
```

이 오류가 나오는 것이 **정상**이다. 실패를 확인하는 것이 이 단계의 목적이다.

**짧은 설명**

Nginx는 설정을 읽는 시점에 `app1`·`app2`라는 이름을 IP로 변환한다. 앱이 멈춰 있으면 Docker 내부 DNS가 그 이름을 찾지 못하므로 변환에 실패한다.

이미 실행 중인 Nginx는 예전에 변환해 둔 IP를 들고 있으므로 계속 동작한다. 문제는 **이 상태에서 `nginx -s reload`를 하면 설정 반영이 실패**한다는 점이다. 따라서 설정 변경은 반드시 앱이 실행 중일 때 한다.

### 6-3. 두 앱을 다시 시작하고 복구 확인

> **이 단계에서 검사하는 것:** 앱을 다시 시작하면 서비스가 `200 OK`로 복구되는지 확인한다.

**입력·실행 — 터미널 B**

```bash
# 중지했던 두 앱을 모두 다시 시작한다.
docker compose start app1 app2

# 두 앱이 완전히 기동될 때까지 기다린다.
sleep 25 && docker compose ps

# 응답이 200으로 복구됐는지 헤더까지 확인한다.
curl -i "http://localhost:8080/owners?study=recovered" | head -5

# 요청 6건을 보내 다시 두 앱에 분산되는지 확인한다.
for i in $(seq 1 6); do
  curl -s -o /dev/null -w "%{http_code}\n" \
    "http://localhost:8080/owners?study=recovered-$i"
done

# 두 앱의 처리 건수를 센다.
grep -Rc "study=recovered-" logs/tomcat/app1
grep -Rc "study=recovered-" logs/tomcat/app2

# 장애 때의 502와 복구 후의 200을 로그에서 함께 비교한다.
docker compose logs --since 15m nginx | grep -E 'study=all-down|study=recovered'
```

**결과·로그 판정**

| 확인 항목 | 정상 값 |
| --- | --- |
| `curl -i` 첫 줄 | `HTTP/1.1 200 OK` |
| 요청 6건 상태 | 모두 `200` |
| 두 앱 건수 합 | `6`, 양쪽 모두 0이 아님 |
| 로그 비교 | `all-down`은 `status=502`, `recovered`는 `status=200` |

복구 후에도 502가 계속 나오면 컨테이너 IP가 바뀌었을 가능성이 있다. 3-3의 IP 조회를 다시 하고, 값이 달라졌다면 Nginx를 다시 읽게 한다.

```bash
# 앱이 모두 실행 중인 지금은 reload가 정상 동작한다.
docker compose exec nginx nginx -t && docker compose exec nginx nginx -s reload
```

**짧은 설명**

이 마지막 확인이 오늘 구조의 의미를 완성한다. `app1`을 멈춰도 서비스가 살아 있고, 다시 시작하면 분산 대상으로 돌아온다. 이 성질이 있으면 **한 대씩 새 버전으로 교체해 중단 없이 배포**할 수 있다.

```text
1. app1 중지 → app2가 전체 처리 (서비스 유지)
2. app1을 새 버전으로 교체·시작
3. app2 중지 → app1(새 버전)이 전체 처리
4. app2를 새 버전으로 교체·시작
→ 사용자는 한 번도 502를 보지 않음
```

로드밸런싱은 장애 대응만이 아니라 무중단 배포의 전제이기도 하다.

---

## 7. (선택) 앱이 느려지는 장애와 타임아웃

> **이 단계에서 검사하는 것:** 앱이 죽지 않고 응답만 하지 않을 때 어떤 일이 생기는지 확인하고, 타임아웃으로 우회를 앞당긴다.

시간이 남을 때만 진행한다. 지금까지 만든 장애는 `stop`으로 **연결이 즉시 거부되는** 경우였다. 실제 장애는 앱이 살아 있으면서 응답만 하지 않는 경우가 더 많다.

### 7-1. 타임아웃 없이 멈춘 앱에 요청 보내기

**입력·실행 — 터미널 B · 위치: PetClinic 폴더**

```bash
# app1의 프로세스를 얼려 응답하지 않는 상태를 만든다. 컨테이너는 살아 있다.
docker compose pause app1

# 요청을 보내고 걸린 시간을 확인한다. 오래 걸릴 수 있다.
curl -s -o /dev/null -w "status=%{http_code} time=%{time_total}s\n" \
  "http://localhost:8080/owners?study=slow-1"

# 얼린 app1을 원래대로 되돌린다.
docker compose unpause app1
```

**명령 한눈에 보기**

| 핵심 구성 | 이 명령에서 하는 역할 |
| --- | --- |
| `docker compose pause` | 컨테이너 안의 프로세스를 **일시 정지**한다. 컨테이너와 포트는 살아 있다. |
| `docker compose unpause` | 정지했던 프로세스를 다시 진행시킨다. |

**결과·로그 판정**

상태는 `200`으로 나오지만 응답 시간이 눈에 띄게 길다. 환경에 따라 연결 단계에서 멈출 수도, 응답을 기다리다 멈출 수도 있다.

**짧은 설명**

`stop`과 달리 `pause`는 연결 자체는 받아들여진다. 그래서 `connect() failed`가 나지 않고, Nginx는 기본값인 **60초까지 기다린다.** 사용자는 그동안 화면을 보지 못한다.

### 7-2. 타임아웃을 넣고 다시 확인

**입력·수정 — 터미널 B**

```bash
# 타임아웃 설정을 추가하기 위해 Nginx 웹 서버 설정 파일을 연다.
nano nginx/conf.d/default.conf
```

두 `location` 안, `proxy_next_upstream` 아래에 추가한다.

```nginx
proxy_connect_timeout 2s;   # 연결 자체를 기다리는 한계
proxy_read_timeout 10s;     # 응답을 기다리는 한계
```

**입력·실행 — 터미널 B**

```bash
# 앱이 모두 실행 중인 상태에서 문법을 검사한다.
docker compose exec nginx nginx -t

# 검사가 통과한 설정을 실행 중인 Nginx에 반영한다.
docker compose exec nginx nginx -s reload

# 다시 app1을 얼리고 같은 요청을 보낸다.
docker compose pause app1
curl -s -o /dev/null -w "status=%{http_code} time=%{time_total}s\n" \
  "http://localhost:8080/owners?study=slow-2"
docker compose unpause app1
```

**결과·로그 판정**

`status=200`은 같지만 **응답 시간이 크게 줄어든다.** 설정한 한계 시간이 지나면 Nginx가 `timeout`을 실패로 판단하고 `proxy_next_upstream` 규칙에 따라 app2로 넘기기 때문이다.

**짧은 설명**

`proxy_next_upstream`에 `timeout`이 들어 있어야 이 우회가 동작한다. 조건 목록의 `timeout`이 바로 이 상황을 위한 것이다.

주의할 점은 이 값이 **너무 짧으면 정상적인 느린 요청까지 실패로 처리**된다는 것이다. 실제 응답 시간을 측정한 뒤 정해야 한다. 그래서 이 단계를 선택으로 둔다.

상태 코드로도 구분된다.

```text
연결이 거부됨  → 502 Bad Gateway
응답이 안 옴   → 504 Gateway Timeout
```

---

## 종료

**입력·실행 — 터미널 B · 위치: PetClinic 폴더**

```bash
# 실습 컨테이너와 네트워크를 종료하되 PostgreSQL 데이터 볼륨은 보존한다.
docker compose down

# 볼륨이 남아 있는지 확인한다.
docker volume ls | grep petclinic
```

`docker compose down -v`는 실행하지 않는다. Named Volume까지 지워 지금까지 쌓은 PetClinic 데이터가 사라진다.

### 오늘의 성공 기준

- [ ] 같은 PetClinic 이미지를 실행하는 `app1`·`app2` 컨테이너가 함께 실행된다.
- [ ] `app1`·`app2`는 호스트 포트를 공개하지 않는다. (`docker compose ps`에서 `8080/tcp`만)
- [ ] 두 앱은 하나의 `postgres:5432`와 기존 Named Volume을 함께 사용한다.
- [ ] `app1`·`app2`의 Tomcat access log가 서로 다른 경로에 저장된다.
- [ ] Nginx가 `upstream`을 통해 요청을 두 앱에 나누어 전달한다. (4-1)
- [ ] 캐시되지 않는 `/owners` 요청이 두 앱의 로그에 나뉘어 기록된다. (4-1)
- [ ] 한쪽 앱을 통해 등록한 데이터를 다른 앱이 처리한 요청에서도 조회할 수 있다. (4-3)
- [ ] 앱 하나를 중지해도 다른 앱이 요청을 처리하여 `200 OK`가 유지된다. (5-1)
- [ ] Nginx access log에서 `upstream`에 주소가 **두 개** 기록된 줄을 찾았다. (5-2)
- [ ] `app1` 중지와 `app2` 중지 **양쪽 방향 모두** 확인했다. (5-1, 5-4)
- [ ] 두 앱을 모두 중지하면 안내 화면과 함께 `502 Bad Gateway`가 반환된다. (6-1)
- [ ] 두 앱을 다시 시작하면 서비스가 `200 OK`로 복구된다. (6-3)


## 부록. 자주 만나는 오류

| 증상 | 원인 | 해결 |
| --- | --- | --- |
| `host not found in upstream "app"` | `proxy_pass`를 한 군데만 바꿨다 | `grep -c 'app:8080' nginx/conf.d/default.conf`가 0인지 확인 (2-2) |
| `host not found in upstream "app1"` | 앱이 멈춘 상태에서 검사·reload했다 | 앱을 먼저 시작한 뒤 다시 실행 (6-2) |
| `nginx -t`가 임시 컨테이너에서 실패 | 네트워크를 붙이지 않았다 | `--network "$NET"` 옵션 확인 (3-1) |
| 로그가 한쪽에만 쌓인다 | 두 앱의 볼륨 경로가 같다 | `grep -n 'logs/tomcat' docker-compose.yml`이 두 줄인지 확인 (1-2) |
| Compose 실행 시 이름 충돌 오류 | `container_name`이 양쪽에 같다 | `container_name`을 다르게 하거나 두 줄 다 지운다 (1-2) |
| 앱을 켰는데 502가 계속된다 | 컨테이너 IP가 바뀌었다 | IP 재확인 후 `nginx -s reload` (6-3) |
| 요청이 5:5로 안 나뉜다 | 정적 파일·브라우저 요청·제외 기간 | `curl`로 `/owners`만 사용 (4-1, 4-2) |
| 두 앱 중 하나가 시작하자마자 죽는다 | `SPRING_SQL_INIT_MODE`가 한쪽에 없다 | 두 서비스 모두에 `never`가 있는지 확인 (1-2) |
| `grep -Rc`가 항상 0 | 로그 폴더를 안 만들었다 | `01-환경세팅.md` 5절 실행 |

