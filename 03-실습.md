# 03. 실습 — 앱 두 대로 늘리고 분산·장애 우회 확인하기

4주차의 Nginx 설정·캐시·보안 헤더·502 안내 화면은 유지한다. 이 문서는 기존 파일을 통째로 덮어쓰지 않고, **바꿀 부분만** 보여 준다. 총 소요 시간은 70분이다.

## 이 문서를 보는 방법

4주차와 같은 화면 구성을 사용한다.

| 표시 | 실제로 작업할 곳 | 하는 일 |
| --- | --- | --- |
| 터미널 A — 로그 관찰 | PetClinic 폴더로 이동한 첫 번째 터미널 | `docker compose logs -f`로 로그 관찰 |
| 터미널 B — 명령 실행 | 같은 PetClinic 폴더를 연 두 번째 터미널 | `docker`, `curl`, `grep` 명령 입력 |
| nano 편집 화면 | 터미널 B에서 `nano 파일경로` 실행 | YAML·Nginx 설정 수정 |
| PC 브라우저 | Chrome·Edge·Safari 등 | `http://localhost:8080` 화면 확인 |

별도 안내가 없는 `bash` 명령은 모두 터미널 B에 입력한다. nano 저장은 `Ctrl+O → Enter → Ctrl+X` 순서다.

> **화면 표시 원칙**
> 각 단계는 `검사 목표 → 입력·수정 → 명령 한눈에 보기 → 결과·로그 판정 → 짧은 설명` 순서로 읽는다. 4주차에서 이미 설명한 `curl -sS`, `logs --since` 같은 기초 옵션은 반복 설명하지 않는다.

## 시간 배분

| 단계 | 내용 | 시간 |
| --- | --- | --- |
| 0 | 4주차 상태 확인 | 5분 |
| 1 | `app`을 `app1`·`app2`로 나누기 | 12분 |
| 2 | Nginx upstream 등록과 전달 대상 변경 | 10분 |
| 3 | 전체 실행과 공개 포트 확인 | 5분 |
| 4 | 요청이 두 앱에 나뉘는지 확인 | 12분 |
| 5 | 앱 하나를 중지하고 장애 우회 확인 | 10분 |
| 6 | 앱을 다시 시작하고 복귀 확인 | 4분 |
| 7 | 모든 앱을 중지하고 502 확인 | 6분 |
| 8 | 전체 복구 확인 | 4분 |
| 9 | 정리와 종료 | 2분 |
| (선택) | 심화 — 느린 앱과 타임아웃 | 10분 |

## 목차

- 0. 시작 상태 확인
- 1. `app`을 `app1`·`app2`로 나누기
- 2. Nginx upstream 등록과 전달 대상 변경
- 3. 전체 실행과 공개 포트 확인
- 4. 요청이 두 앱에 나뉘는지 확인
- 5. 앱 하나를 중지하고 장애 우회 확인
- 6. 앱을 다시 시작하고 복귀 확인
- 7. 모든 앱을 중지하고 502 확인
- 8. 전체 복구 확인
- 9. 정리와 종료
- (선택) 심화. 느린 앱과 타임아웃

---

## 0. 시작 상태 확인 (5분)

> **이 단계에서 검사하는 것:** 4주차 구성이 정상 동작하는 상태에서 시작하는지, 수정할 파일과 로그 폴더가 준비됐는지 확인한다.

**입력·실행 — 터미널 B · 위치: PetClinic 폴더**

```bash
# 현재 위치가 PetClinic 폴더인지 확인한다.
ls docker-compose.yml nginx/conf.d/default.conf

# 5주차에서 사용할 앱별 로그 폴더가 준비됐는지 확인한다.
ls -ld logs/tomcat/app1/access logs/tomcat/app2/access

# 4주차 default.conf에 proxy_pass가 몇 군데 있는지 확인한다.
grep -n 'proxy_pass' nginx/conf.d/default.conf

# 4주차 구성을 실행해 정상 동작을 확인한다.
docker compose up -d postgres app nginx
docker compose ps
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8080/
```

**명령 한눈에 보기**

| 핵심 구성 | 이 명령에서 하는 역할 |
| --- | --- |
| `grep -n 'proxy_pass'` | 줄 번호와 함께 전달 설정의 위치를 모두 찾는다. |
| `up -d postgres app nginx` | 4주차 세 서비스를 백그라운드로 실행한다. |
| `-w "%{http_code}\n"` | 응답 본문 대신 HTTP 상태 코드만 출력한다. |

**결과·로그 판정**

`proxy_pass`가 **두 줄** 나와야 한다. 하나는 정적 파일 `location`, 하나는 일반 `location /`이다. 마지막 명령이 `200`이면 4주차 상태가 정상이다.

**짧은 설명**

이 두 줄을 모두 바꾸는 것이 2단계의 핵심이다. 하나라도 놓치면 Nginx가 존재하지 않는 `app`을 찾다가 시작하지 못한다. 확인이 끝나면 다음 명령으로 컨테이너를 내린다.

```bash
# 서비스 구성을 바꾸기 전에 컨테이너를 내린다. DB 볼륨은 보존한다.
docker compose down
```

---

## 1. `app`을 `app1`·`app2`로 나누기 (12분)

### 1-1. 기존 `app` 서비스를 `app1`로 바꾸기

> **이 단계에서 검사하는 것:** 기존 `app` 서비스의 이름과 로그 저장 위치만 바꿔 첫 번째 앱 컨테이너로 만든다.

**입력·수정 — 터미널 B**

```bash
# app 서비스의 이름과 볼륨 경로를 바꾸기 위해 Compose 파일을 연다.
nano docker-compose.yml
```

nano 화면에서 `services.app` 블록을 찾아 **두 줄만** 바꾼다.

```yaml
# 바꾸기 전
  app:
    ...
    volumes:
      - ./logs/tomcat:/tmp/tomcat

# 바꾼 뒤
  app1:
    ...
    volumes:
      - ./logs/tomcat/app1:/tmp/tomcat
```

나머지 항목(`image`, `environment`, `expose`, `depends_on`, `networks`)은 **전혀 건드리지 않는다.**

**설정 한눈에 보기**

| 바뀌는 것 | 이유 |
| --- | --- |
| `app:` → `app1:` | Nginx가 `app1`이라는 이름으로 이 컨테이너를 찾게 한다. |
| `./logs/tomcat` → `./logs/tomcat/app1` | 이 앱의 Tomcat 로그만 별도 폴더에 저장한다. |
| 컨테이너 안 경로 `/tmp/tomcat` | 바꾸지 않는다. 컨테이너 안에서는 두 앱이 완전히 동일하다. |

**결과·로그 판정**

여기서는 저장만 한다. 문법은 1-3의 `docker compose config`에서 확인한다.

**짧은 설명**

이름과 로그 위치만 바꾸는 것이 전부다. 앱 자체는 4주차와 완전히 같은 이미지·같은 설정으로 실행된다.

### 1-2. `app2` 서비스 추가

> **이 단계에서 검사하는 것:** `app1`과 동일한 두 번째 앱 컨테이너를 정의하고, 로그 저장 위치만 다르게 지정한다.

**입력·수정 — 터미널 B**

```bash
# 두 번째 앱 서비스를 추가하기 위해 Compose 파일을 연다.
nano docker-compose.yml
```

nano 화면에서 `app1:` 블록 바로 아래에 다음 내용을 추가한다. `app1`과 같은 깊이(들여쓰기 2칸)에 둔다.

```yaml
  app2:
    image: spring-petclinic:dockerfile
    environment:
      SPRING_PROFILES_ACTIVE: postgres
      POSTGRES_URL: jdbc:postgresql://postgres:5432/petclinic
      POSTGRES_USER: petclinic
      POSTGRES_PASS: petclinic
      SPRING_SQL_INIT_MODE: never
      SERVER_TOMCAT_BASEDIR: /tmp/tomcat
      SERVER_TOMCAT_ACCESSLOG_ENABLED: "true"
      SERVER_TOMCAT_ACCESSLOG_DIRECTORY: access
      SERVER_TOMCAT_ACCESSLOG_PATTERN: common
      SERVER_TOMCAT_ACCESSLOG_BUFFERED: "false"
    expose: ["8080"]
    volumes:
      - ./logs/tomcat/app2:/tmp/tomcat
    depends_on:
      postgres:
        condition: service_healthy
    networks: [proxy-net, db-net]
```

같은 nano 화면에서 `services.nginx`의 `depends_on`도 바꾼다.

```yaml
# 바꾸기 전
    depends_on: [app]

# 바꾼 뒤
    depends_on: [app1, app2]
```

저장하고 나온다.

**설정 한눈에 보기**

| 설정 | 이 실습에서 하는 역할 |
| --- | --- |
| `image` | `app1`과 **같은 이미지**를 쓴다. 같은 애플리케이션이라는 뜻이다. |
| `POSTGRES_URL` | `app1`과 같은 `postgres:5432`를 가리킨다. 두 앱이 같은 DB를 공유한다. |
| `SPRING_SQL_INIT_MODE: never` | 두 앱이 동시에 시작하며 테이블을 중복 생성하지 않게 한다. |
| `volumes` | `app1`과 다른 유일한 항목이다. 호스트의 `app2` 폴더에 로그를 저장한다. |
| `depends_on` | PostgreSQL 준비 완료 후 시작한다. `app1`과 동일하다. |
| `nginx.depends_on` | Nginx가 두 앱을 모두 기다린 뒤 시작하게 한다. |

**결과·로그 판정**

저장 후 `app1:`과 `app2:`가 같은 들여쓰기 깊이인지 확인한다. 한쪽이 다른 쪽 안으로 들어가면 안 된다.

**짧은 설명**

두 블록의 차이는 이름과 볼륨 경로뿐이다. 나중에 앱을 세 개로 늘린다면 같은 방식으로 `app3`을 추가하면 된다.

### 1-3. Compose 설정 검사

> **이 단계에서 검사하는 것:** 두 앱이 올바르게 정의됐고 호스트 포트를 열지 않는지 실행 전에 확인한다.

**입력·실행 — 터미널 B**

```bash
# 합쳐진 최종 Compose 설정에 문법 오류가 없는지 확인한다.
docker compose config >/dev/null && echo 문법정상

# 정의된 서비스 목록에 app1, app2가 모두 있는지 확인한다.
docker compose config --services | sort

# 두 앱의 로그 볼륨 경로가 서로 다른지 확인한다.
docker compose config | grep -E 'logs/tomcat'

# 호스트에 공개되는 포트가 8080 하나뿐인지 확인한다.
docker compose config | grep -B 3 'published'
```

**명령 한눈에 보기**

| 핵심 구성 | 이 명령에서 하는 역할 |
| --- | --- |
| `config >/dev/null` | 출력은 버리고 문법 오류만 확인한다. |
| `config --services` | 정의된 서비스 이름만 목록으로 출력한다. |
| `grep -E 'logs/tomcat'` | 최종 설정에서 로그 볼륨 경로만 뽑아 본다. |
| `grep -B 3 'published'` | 호스트에 공개된 포트와 그 앞 3줄을 함께 본다. |

**결과·로그 판정**

`문법정상`이 출력되고, 서비스 목록에 `app1`, `app2`, `nginx`, `postgres`가 보여야 한다. `app`이 남아 있으면 1-1의 이름 변경이 반영되지 않은 것이다.

로그 볼륨은 `logs/tomcat/app1`과 `logs/tomcat/app2` 두 줄이 나와야 한다. 같은 경로가 두 번 나오면 1-2에서 볼륨 경로를 바꾸지 않은 것이므로 두 앱의 로그가 섞이게 된다.

공개 포트는 `published: "8080"`이 **한 번만** 나와야 한다. 두 번 이상 나오면 앱에 `ports:`가 붙어 있는 것이다.

**짧은 설명**

`docker compose config`는 기본 파일과 override를 합친 **최종 결과**를 보여 준다. 실행 전에 여기서 잡히는 오류를 먼저 해결하면 시간을 아낄 수 있다.

---

## 2. Nginx upstream 등록과 전달 대상 변경 (10분)

### 2-1. upstream 블록 추가

> **이 단계에서 검사하는 것:** 두 앱을 하나의 서버 묶음으로 등록하고 실패 감지 기준을 설정한다.

**입력·수정 — 터미널 B**

```bash
# 앱 서버 묶음을 등록하기 위해 Nginx 웹 서버 설정 파일을 연다.
nano nginx/conf.d/default.conf
```

nano 화면에서 파일 **맨 위**, `server {` 줄보다 위에 다음 내용을 추가한다.

```nginx
upstream petclinic_backend {
  server app1:8080 max_fails=1 fail_timeout=10s;
  server app2:8080 max_fails=1 fail_timeout=10s;
}
```

**설정 한눈에 보기**

| 설정 | 이 실습에서 하는 역할 |
| --- | --- |
| `upstream petclinic_backend` | 앱 서버 묶음에 이름을 붙인다. 이 이름을 `proxy_pass`에서 쓴다. |
| `server app1:8080` | 묶음에 첫 번째 앱을 등록한다. `app1`은 Compose 서비스명이다. |
| `max_fails=1` | 연결 실패 1회면 이 앱을 제외 대상으로 표시한다. |
| `fail_timeout=10s` | 실패를 세는 기간이자, 제외한 앱을 건너뛰는 기간이다. |
| 위치가 `server {}` 바깥 | `upstream`은 `http` 범위 설정이므로 `server` 안에 넣으면 오류가 난다. |

**결과·로그 판정**

여기서는 저장만 한다. 문법은 3단계에서 `nginx -t`로 확인한다.

**짧은 설명**

`max_fails=1`은 결과를 빨리 보기 위한 학습용 값이다. 실제 운영에서는 일시적 오류로 앱이 쉽게 제외되지 않도록 더 큰 값을 쓴다.

### 2-2. 전달 대상 변경과 장애 우회 설정

> **이 단계에서 검사하는 것:** 두 `location`의 전달 대상을 upstream 이름으로 바꾸고, 실패 시 다른 앱을 시도하도록 설정한다.

**입력·수정 — 터미널 B**

같은 nano 화면에서 계속 작업한다. `proxy_pass`가 있는 **두 곳을 모두** 바꾼다.

```nginx
# 정적 파일 location 안 — 바꾸기 전
    proxy_pass http://app:8080;
# 바꾼 뒤
    proxy_pass http://petclinic_backend;

# 일반 location / 안 — 바꾸기 전
    proxy_pass http://app:8080;
# 바꾼 뒤
    proxy_pass http://petclinic_backend;
```

그리고 `location / {` 안, `proxy_intercept_errors on;` 아래에 다음 두 줄을 추가한다.

```nginx
    proxy_next_upstream error timeout http_502 http_503 http_504;
    proxy_next_upstream_tries 2;
```

저장하고 나온다.

**설정 한눈에 보기**

| 설정 | 이 실습에서 하는 역할 |
| --- | --- |
| `proxy_pass http://petclinic_backend` | 고정된 주소 대신 앱 묶음으로 요청을 보낸다. |
| `proxy_next_upstream error timeout` | 연결 오류나 시간 초과일 때 다른 앱을 시도한다. |
| `http_502 http_503 http_504` | 앱이 이 상태 코드를 반환해도 다른 앱을 시도한다. |
| `proxy_next_upstream_tries 2` | 한 요청에 대해 최대 2개 앱까지만 시도한다. |

`http_500`이나 `http_404`를 넣지 않는 이유는 앱이 정상 동작하며 판단한 결과이기 때문이다. 다른 앱에 보내도 같은 결과가 나온다.

**입력·실행 — 터미널 B · 변경 확인**

```bash
# app:8080이 남아 있지 않은지 확인한다. 아무것도 안 나와야 정상이다.
grep -n 'app:8080' nginx/conf.d/default.conf

# upstream으로 바뀐 전달 설정이 두 군데인지 확인한다.
grep -n 'proxy_pass http://petclinic_backend' nginx/conf.d/default.conf

# upstream 블록이 server 블록보다 위에 있는지 줄 번호로 확인한다.
grep -nE '^upstream|^server' nginx/conf.d/default.conf
```

**결과·로그 판정**

첫 명령은 **아무 결과도 나오지 않아야** 한다. 두 번째는 두 줄이 나와야 한다. 세 번째에서 `upstream`의 줄 번호가 `server`보다 작아야 한다.

**짧은 설명**

첫 명령에서 결과가 나오면 바꾸지 않은 `proxy_pass`가 남아 있다는 뜻이다. 그 상태로 실행하면 Nginx가 `host not found in upstream "app"` 오류로 시작하지 못한다. 3단계로 넘어가기 전에 반드시 해결한다.

---

## 3. 전체 실행과 공개 포트 확인 (5분)

> **이 단계에서 검사하는 것:** 네 서비스가 모두 실행되고, 호스트에 공개된 포트는 Nginx의 8080 하나뿐인지 확인한다.

**입력·실행 — 터미널 B**

```bash
# DB와 두 앱을 먼저 실행한다.
docker compose up -d postgres app1 app2

# 두 앱이 모두 기동 완료됐는지 확인한다. Started 문구가 각각 나와야 한다.
docker compose logs --tail 5 app1 app2 | grep -E 'Started|ERROR'

# 앱이 실행 중인 상태에서 Nginx 설정 문법을 검사한다.
docker compose run --rm --no-deps nginx nginx -t

# 문법 검사가 성공하면 Nginx를 다시 만들어 새 설정을 적용한다.
docker compose up -d --force-recreate nginx

# 네 서비스의 실행 상태와 호스트 공개 포트를 확인한다.
docker compose ps
```

**명령 한눈에 보기**

| 핵심 구성 | 이 명령에서 하는 역할 |
| --- | --- |
| `up -d postgres app1 app2` | Nginx보다 앱을 먼저 띄운다. Nginx가 시작할 때 앱 이름을 찾아야 하기 때문이다. |
| `run --rm --no-deps nginx nginx -t` | 임시 컨테이너에서 설정만 검사하고 바로 삭제한다. |
| `--force-recreate nginx` | 기존 Nginx 컨테이너를 지우고 새 설정으로 다시 만든다. |
| `docker compose ps` | 각 서비스의 상태와 `PORTS` 열을 보여 준다. |

**결과·로그 판정**

`nginx -t`는 `test is successful`을 출력해야 한다. `docker compose ps`의 `PORTS` 열은 다음과 같아야 한다.

```text
nginx      → 0.0.0.0:8080->80/tcp
app1       → 8080/tcp
app2       → 8080/tcp
postgres   → 5432/tcp
```

`app1`·`app2`·`postgres`에는 `0.0.0.0:`으로 시작하는 부분이 없어야 한다. 있으면 호스트에 노출된 것이다.

**입력·실행 — 터미널 B · 첫 요청 확인**

```bash
# Nginx를 통해 PetClinic 첫 화면이 정상 응답하는지 확인한다.
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8080/

# 두 앱의 컨테이너 IP를 확인해 로그의 upstream 주소와 대응시킬 준비를 한다.
docker inspect -f '{{.Name}} {{range .NetworkSettings.Networks}}{{.IPAddress}} {{end}}' \
  $(docker compose ps -q app1 app2)
```

**결과·로그 판정**

상태 코드가 `200`이면 정상이다. 두 번째 명령은 컨테이너 이름과 IP를 출력한다. 이 값을 메모해 두면 4단계에서 Nginx 로그의 `upstream` 값을 읽기 편하다.

```text
/petclinic-app1-1  172.18.0.4  172.19.0.3
/petclinic-app2-1  172.18.0.5  172.19.0.4
```

**짧은 설명**

앱마다 IP가 두 개씩 나오는 것은 `proxy-net`과 `db-net` 두 네트워크에 연결돼 있기 때문이다. Nginx 로그에 나타나는 것은 `proxy-net` 쪽 주소다.

> 앱이 `Started` 없이 종료되고 `relation ... already exists`가 보이면 프록시 문제가 아니라 초기화 SQL 중복 문제다. 두 앱 모두에 `SPRING_SQL_INIT_MODE: never`가 있는지 확인한다.

---

## 4. 요청이 두 앱에 나뉘는지 확인 (12분)

### 4-1. 동적 경로로 반복 요청 보내기

> **이 단계에서 검사하는 것:** 캐시되지 않는 경로로 요청을 여러 번 보내 라운드 로빈 분산이 실제로 일어나는지 확인한다.

**입력·실행 — 터미널 B**

```bash
# 이번 시험 요청을 로그에서 찾기 위한 표식을 정한다.
LB_ID=week5-$(date +%H%M%S)
echo "$LB_ID"

# 캐시하지 않는 /owners 경로로 요청 10건을 순서대로 보낸다.
for i in $(seq 1 10); do
  curl -s -o /dev/null -w "%{http_code}\n" \
    "http://localhost:8080/owners?study=$LB_ID-$i"
done
```

**명령 한눈에 보기**

| 핵심 구성 | 이 명령에서 하는 역할 |
| --- | --- |
| `LB_ID=week5-$(date +%H%M%S)` | 시·분·초를 붙여 이전 실습과 겹치지 않는 표식을 만든다. |
| `for i in $(seq 1 10)` | 1부터 10까지 반복한다. |
| `?study=$LB_ID-$i` | 각 요청에 고유 표식을 붙여 로그에서 찾을 수 있게 한다. |
| `-s -o /dev/null` | 진행 표시와 응답 본문을 감춘다. |

**결과·로그 판정**

`200`이 10번 출력되어야 한다. `302`나 `404`가 나오면 PetClinic 버전에 따라 경로가 다른 것이므로 `/owners/find`로 바꿔 다시 시도한다.

**짧은 설명**

정적 이미지를 쓰면 Nginx 캐시에서 응답이 끝나 앱까지 가지 않는다. 그래서 분산 확인에는 DB를 조회하는 동적 경로를 쓴다.

### 4-2. 앱별 처리 건수 세기

> **이 단계에서 검사하는 것:** 두 앱의 Tomcat access log에 요청이 나뉘어 기록됐는지 개수로 확인한다.

**입력·실행 — 터미널 B**

```bash
# app1이 처리한 이번 시험 요청 건수를 센다.
grep -h "$LB_ID" logs/tomcat/app1/access/* | wc -l

# app2가 처리한 이번 시험 요청 건수를 센다.
grep -h "$LB_ID" logs/tomcat/app2/access/* | wc -l

# 어느 요청 번호를 어느 앱이 처리했는지 순서대로 확인한다.
grep -ho "$LB_ID-[0-9]*" logs/tomcat/app1/access/* | sed 's/$/  → app1/'
grep -ho "$LB_ID-[0-9]*" logs/tomcat/app2/access/* | sed 's/$/  → app2/'
```

**명령 한눈에 보기**

| 핵심 구성 | 이 명령에서 하는 역할 |
| --- | --- |
| `grep -h` | 파일 이름을 앞에 붙이지 않고 내용만 출력한다. |
| `logs/tomcat/app1/access/*` | 그 폴더의 모든 로그 파일을 대상으로 한다. |
| `wc -l` | 줄 수를 세어 처리 건수를 구한다. |
| `grep -o` | 조건에 맞는 부분만 잘라 출력한다. |
| `sed 's/$/ → app1/'` | 각 줄 끝에 어느 앱인지 표시를 붙인다. |

**결과·로그 판정**

두 개수의 합이 10이어야 한다. 보통 5와 5로 나뉜다. 4대 6이나 6대 4가 나와도 분산 자체는 성공이다.

한쪽이 10, 다른 쪽이 0이면 분산이 되지 않은 것이다. 이때는 `default.conf`의 `proxy_pass`가 upstream을 가리키는지 다시 확인한다.

**짧은 설명**

세 번째·네 번째 명령의 출력을 번갈아 보면 홀수 번 요청과 짝수 번 요청이 각각 다른 앱에 갔음을 알 수 있다. 이것이 라운드 로빈이다.

### 4-3. Nginx 로그의 upstream 확인

> **이 단계에서 검사하는 것:** Nginx가 실제로 어느 주소에 전달했는지 access log에서 확인하고, 앱 IP와 대응시킨다.

**입력·실행 — 터미널 B**

```bash
# 이번 시험 요청에서 Nginx가 전달한 주소별 건수를 센다.
docker compose logs --since 5m --no-log-prefix nginx | \
  grep "$LB_ID" | grep -o 'upstream="[^"]*"' | sort | uniq -c

# 로그 한 줄의 전체 형태를 확인한다.
docker compose logs --since 5m --no-log-prefix nginx | grep "$LB_ID-1"
```

**명령 한눈에 보기**

| 핵심 구성 | 이 명령에서 하는 역할 |
| --- | --- |
| `--no-log-prefix` | Compose가 붙이는 서비스명 접두사를 뺀다. |
| `grep -o 'upstream="[^"]*"'` | 로그 줄에서 `upstream="..."` 부분만 잘라 낸다. |
| `sort \| uniq -c` | 같은 값끼리 모아 개수를 센다. |

**결과·로그 판정**

두 개의 IP가 각각 몇 건씩 나온다. 3단계에서 메모한 컨테이너 IP와 비교하면 어느 앱인지 알 수 있다.

```text
   5 upstream="172.18.0.4:8080"    ← app1
   5 upstream="172.18.0.5:8080"    ← app2
```

**짧은 설명**

Nginx는 설정을 읽는 시점에 `app1`이라는 이름을 IP로 바꿔 둔다. 그래서 로그에는 이름이 아니라 IP가 기록된다. 이 값이 4-2의 Tomcat 로그 개수와 일치하면 두 로그가 서로를 검증해 준 것이다.

### 4-4. 두 앱이 같은 데이터를 보는지 확인 (선택)

> **이 단계에서 검사하는 것:** 어느 앱이 처리하든 같은 PostgreSQL 데이터가 조회되는지 확인한다.

**입력·실행 — PC 브라우저와 터미널 B**

브라우저에서 `http://localhost:8080/owners/new`를 열어 소유자를 한 명 등록한다. 그다음 아래를 실행한다.

```bash
# 새로 등록한 소유자가 여러 번 조회해도 계속 나오는지 확인한다.
for i in 1 2 3 4; do
  curl -s "http://localhost:8080/owners?lastName=&study=$LB_ID-db-$i" | \
    grep -c '<tr>'
done
```

**결과·로그 판정**

네 번의 출력이 모두 같은 숫자여야 한다. 요청은 두 앱에 나뉘어 갔지만 같은 DB를 보므로 결과가 같다.

**짧은 설명**

앱이 데이터를 자기 안에 갖고 있지 않고 DB에서 읽기 때문에 가능한 일이다. 만약 앱이 메모리에 상태를 저장했다면 요청마다 다른 결과가 나왔을 것이다.

---

## 5. 앱 하나를 중지하고 장애 우회 확인 (10분)

> **이 단계에서 검사하는 것:** `app1`을 멈춰도 사용자 응답은 `200 OK`로 유지되는지, 그리고 그 사실이 로그에 어떻게 남는지 확인한다.

**입력·실행 — 터미널 A · 로그 관찰 시작**

```bash
# Nginx의 새 로그를 실시간으로 관찰한다.
docker compose logs -f --since 10s nginx
```

**입력·실행 — 터미널 B · 앱 하나 중지**

```bash
# 장애 상황을 만들기 위해 app1만 중지한다.
docker compose stop app1

# 두 서비스의 상태 차이를 확인한다.
docker compose ps app1 app2

# 이번 장애 시험용 표식을 정한다.
DOWN_ID=app1down-$(date +%H%M%S)

# app1이 멈춘 상태에서 요청 6건을 보낸다.
for i in $(seq 1 6); do
  curl -s -o /dev/null -w "%{http_code}\n" \
    "http://localhost:8080/owners?study=$DOWN_ID-$i"
done
```

**명령 한눈에 보기**

| 핵심 구성 | 이 명령에서 하는 역할 |
| --- | --- |
| `stop app1` | 컨테이너를 삭제하지 않고 멈추기만 한다. |
| `ps app1 app2` | 한쪽은 `exited`, 다른 쪽은 `running`으로 표시된다. |
| 요청 6건 | 원래라면 3건이 `app1`으로 갔어야 할 만큼 보낸다. |

**결과·로그 판정**

여섯 번 모두 `200`이 나와야 한다. `app1`이 멈췄는데도 서비스가 유지되는 것이다.

**입력·실행 — 터미널 B · 로그로 원인 확인**

```bash
# Nginx error log에서 app1 연결 실패 기록을 찾는다.
docker compose logs --since 3m --no-log-prefix nginx | \
  grep -Ei 'connect\(\) failed|no live upstreams|upstream server temporarily disabled'

# 첫 요청의 upstream 값을 확인한다. 주소가 두 개면 우회가 일어난 것이다.
docker compose logs --since 3m --no-log-prefix nginx | grep "$DOWN_ID-1"

# 살아 있는 app2가 6건을 모두 처리했는지 확인한다.
grep -h "$DOWN_ID" logs/tomcat/app2/access/* | wc -l

# 멈춘 app1에는 기록이 없는지 확인한다. 0이어야 한다.
grep -h "$DOWN_ID" logs/tomcat/app1/access/* | wc -l
```

**결과·로그 판정**

| 확인 대상 | 기대 결과 |
| --- | --- |
| `curl` 상태 코드 | 6건 모두 `200` |
| Nginx error log | `connect() failed (111: Connection refused)` 등이 기록됨 |
| 첫 요청의 `upstream` | 주소 두 개가 쉼표로 이어짐 (예: `"172.18.0.4:8080, 172.18.0.5:8080"`) |
| `app2` 로그 건수 | 6 |
| `app1` 로그 건수 | 0 |

첫 요청에만 주소가 두 개고 나머지는 하나인 것이 정상이다. `max_fails=1`로 `app1`이 제외됐기 때문에 이후 요청은 처음부터 `app2`로 간다.

**짧은 설명**

여기가 오늘 실습의 핵심 장면이다. 사용자는 `200`만 받았기 때문에 장애를 전혀 눈치채지 못한다. 그러나 error log에는 실패 기록이 남아 있다. **응답 화면만 보면 장애를 알 수 없고, 로그를 봐야 안다.**

한 요청이 실패를 감지하는 대가를 치렀다는 점도 확인해 둔다. 이것이 능동 헬스체크가 아니라 수동 감지 방식의 특징이다.

> `app1`이 멈춘 동안에는 `nginx -s reload`를 실행하지 않는다. Nginx가 `app1` 이름을 찾지 못해 `host not found in upstream` 오류로 설정 반영에 실패한다.

---

## 6. 앱을 다시 시작하고 복귀 확인 (4분)

> **이 단계에서 검사하는 것:** 멈췄던 앱을 다시 시작하면 자동으로 분산 대상에 복귀하는지 확인한다.

**입력·실행 — 터미널 B**

```bash
# 중지했던 app1을 다시 시작한다.
docker compose start app1

# app1이 기동 완료됐는지 확인한다.
docker compose logs --tail 5 app1 | grep -E 'Started|ERROR'

# fail_timeout 10초가 지나도록 잠시 기다린다.
sleep 12

# 복귀 확인용 표식을 정하고 요청 8건을 보낸다.
BACK_ID=recovered-$(date +%H%M%S)
for i in $(seq 1 8); do
  curl -s -o /dev/null -w "%{http_code}\n" \
    "http://localhost:8080/owners?study=$BACK_ID-$i"
done

# 두 앱이 다시 나누어 처리하는지 확인한다.
grep -h "$BACK_ID" logs/tomcat/app1/access/* | wc -l
grep -h "$BACK_ID" logs/tomcat/app2/access/* | wc -l
```

**명령 한눈에 보기**

| 핵심 구성 | 이 명령에서 하는 역할 |
| --- | --- |
| `start app1` | 멈춘 컨테이너를 다시 실행한다. 새로 만들지 않는다. |
| `sleep 12` | `fail_timeout=10s`가 지나 `app1`이 다시 시도 대상이 되도록 기다린다. |
| 요청 8건 | 두 앱에 4건씩 나뉘는지 볼 수 있을 만큼 보낸다. |

**결과·로그 판정**

두 앱의 건수 합이 8이고, 양쪽 모두 0이 아니어야 한다. 설정을 다시 만지지 않았는데도 `app1`이 스스로 복귀한 것이 핵심이다.

`app1`이 0이면 `fail_timeout`이 아직 지나지 않았거나 앱 기동이 끝나지 않은 것이다. 잠시 기다린 뒤 다시 요청한다.

**짧은 설명**

`fail_timeout=10s`는 제외 기간이기도 하다. 10초가 지나면 Nginx가 그 앱에 다시 요청을 보내 보고, 성공하면 정상 대상으로 되돌린다. 복귀에 별도 조작이 필요 없다.

---

## 7. 모든 앱을 중지하고 502 확인 (6분)

> **이 단계에서 검사하는 것:** 전달할 앱이 하나도 없을 때 4주차에서 만든 안내 화면이 나오면서도 HTTP 상태는 `502`로 유지되는지 확인한다.

**입력·실행 — 터미널 B**

```bash
# 두 앱을 모두 중지한다.
docker compose stop app1 app2

# nginx와 postgres만 남았는지 확인한다.
docker compose ps

# 상태 코드와 응답 헤더를 함께 확인한다.
curl --max-time 20 -i "http://localhost:8080/owners?study=all-down" | head -n 12

# 안내 화면의 문구가 실제로 나오는지 확인한다.
curl -s --max-time 20 "http://localhost:8080/owners?study=all-down-2" | grep -i 'PetClinic'
```

**명령 한눈에 보기**

| 핵심 구성 | 이 명령에서 하는 역할 |
| --- | --- |
| `stop app1 app2` | 두 앱을 함께 멈춘다. Nginx와 DB는 유지한다. |
| `-i` | 응답 본문 앞에 상태 코드와 헤더를 함께 출력한다. |
| `head -n 12` | 출력이 길어지지 않도록 앞부분만 본다. |
| `--max-time 20` | 응답이 20초를 넘으면 중단한다. |

**결과·로그 판정**

첫 줄이 `HTTP/1.1 502 Bad Gateway`여야 한다. 그리고 본문에는 4주차에서 만든 안내 문구가 나와야 한다.

```text
HTTP/1.1 502 Bad Gateway
...
잠시 PetClinic에 연결할 수 없습니다
```

**입력·실행 — 터미널 B · 원인 로그 확인**

```bash
# 모든 앱이 제외된 상황을 나타내는 error log를 찾는다.
docker compose logs --since 2m --no-log-prefix nginx | \
  grep -Ei 'no live upstreams|connect\(\) failed'

# 어느 앱에도 요청이 도달하지 않았는지 확인한다. 둘 다 0이어야 한다.
grep -h 'all-down' logs/tomcat/app1/access/* | wc -l
grep -h 'all-down' logs/tomcat/app2/access/* | wc -l
```

**결과·로그 판정**

| 확인 대상 | 기대 결과 |
| --- | --- |
| HTTP 상태 | `502` |
| 화면 | 4주차 안내 문구 |
| error log | `no live upstreams` 또는 `connect() failed` |
| 두 앱의 로그 | 각각 0건 |

**짧은 설명**

화면은 사용자에게 친절하게 바뀌었지만 상태 코드는 `502` 그대로다. 여기서 상태를 `200`으로 바꾸면 모니터링 도구가 장애를 정상으로 오해한다. 화면과 상태 코드는 별개라는 점이 이 단계의 요지다.

`no live upstreams`는 앱이 두 대 이상일 때만 볼 수 있는 메시지다. `max_fails`로 두 앱이 모두 제외 표시된 뒤에 들어온 요청에서 나타난다.

---

## 8. 전체 복구 확인 (4분)

> **이 단계에서 검사하는 것:** 두 앱을 모두 다시 시작하면 서비스가 원래대로 돌아오는지 확인한다.

**입력·실행 — 터미널 B**

```bash
# 두 앱을 다시 시작한다.
docker compose start app1 app2

# 네 서비스가 모두 실행 중인지 확인한다.
docker compose ps

# 두 앱이 모두 기동 완료됐는지 확인한다.
docker compose logs --tail 5 app1 app2 | grep -E 'Started|ERROR'

# 제외 기간이 지나도록 기다린 뒤 요청을 보낸다.
sleep 12
FINAL_ID=final-$(date +%H%M%S)
for i in $(seq 1 6); do
  curl -s -o /dev/null -w "%{http_code}\n" \
    "http://localhost:8080/owners?study=$FINAL_ID-$i"
done

# 두 앱이 다시 나누어 처리하는지 확인한다.
grep -h "$FINAL_ID" logs/tomcat/app1/access/* | wc -l
grep -h "$FINAL_ID" logs/tomcat/app2/access/* | wc -l
```

**결과·로그 판정**

6건 모두 `200`이고, 두 앱의 건수 합이 6이며 양쪽 모두 0이 아니어야 한다. 브라우저에서 `http://localhost:8080/`을 열었을 때 PetClinic 첫 화면이 나오면 완전히 복구된 것이다.

**짧은 설명**

4주차와 달리 복구 과정에서 설정을 고치지 않았다. 앱을 켜기만 하면 Nginx가 알아서 다시 분산 대상에 포함시킨다.

---

## 9. 정리와 종료 (2분)

**입력·실행 — 터미널 B**

```bash
# 실습 컨테이너와 네트워크를 종료하되 PostgreSQL 데이터 볼륨은 보존한다.
docker compose down
```

`-v`를 붙이지 않는다. 데이터 볼륨이 유지되므로 다음 실행 때 기존 데이터가 다시 연결된다.

터미널 A에서 로그를 보고 있었다면 `Ctrl+C`로 종료한다.

## 성공 기준

- [ ] 같은 PetClinic 이미지를 실행하는 `app1`·`app2` 컨테이너가 함께 실행된다.
- [ ] `app1`·`app2`는 호스트 포트를 공개하지 않고, 공개 포트는 Nginx의 `8080` 하나다.
- [ ] 두 앱이 하나의 `postgres:5432`와 기존 Named Volume을 함께 사용한다.
- [ ] `app1`·`app2`의 Tomcat access log가 서로 다른 폴더에 저장된다.
- [ ] Nginx가 `upstream`을 통해 요청을 두 앱에 나누어 전달한다.
- [ ] 캐시되지 않는 `/owners` 요청이 두 앱의 로그에 나뉘어 기록된다.
- [ ] 앱 하나를 중지해도 다른 앱이 처리하여 `200 OK`가 유지된다.
- [ ] 우회가 일어난 요청의 Nginx 로그 `upstream`에 주소가 두 개 기록된다.
- [ ] 두 앱을 모두 중지하면 안내 화면과 함께 `502 Bad Gateway`가 반환된다.
- [ ] 앱을 다시 시작하면 설정 변경 없이 자동으로 분산 대상에 복귀한다.

---

## (선택) 심화. 느린 앱과 타임아웃 (10분)

> **이 단계에서 검사하는 것:** 앱이 죽지 않고 응답만 느릴 때는 지금 설정으로 우회가 되지 않는다는 사실을 확인한다.

`docker compose stop`은 연결이 즉시 거부되는 상황이다. 그런데 실제 장애는 앱이 살아 있으면서 응답만 하지 않는 경우가 더 많다. 이때 Nginx는 기본값인 60초까지 기다린다.

**입력·수정 — 터미널 B**

```bash
# 기다리는 한계를 정하기 위해 Nginx 웹 서버 설정 파일을 연다.
nano nginx/conf.d/default.conf
```

`location / {` 안에 다음 두 줄을 추가한다.

```nginx
    proxy_connect_timeout 2s;
    proxy_read_timeout 10s;
```

**입력·실행 — 터미널 B**

```bash
# 두 앱이 실행 중인 상태에서 설정을 검사하고 반영한다.
docker compose exec nginx nginx -t
docker compose exec nginx nginx -s reload

# 설정이 실제로 반영됐는지 확인한다.
docker compose exec nginx grep -n 'timeout' /etc/nginx/conf.d/default.conf
```

**설정 한눈에 보기**

| 설정 | 이 실습에서 하는 역할 |
| --- | --- |
| `proxy_connect_timeout 2s` | 앱에 TCP 연결을 만드는 데 2초를 넘기면 실패로 처리한다. |
| `proxy_read_timeout 10s` | 앱이 응답을 보내지 않고 10초가 지나면 실패로 처리한다. |

**결과·로그 판정**

`nginx -t`가 성공하고 reload가 오류 없이 끝나야 한다. 일반 요청이 여전히 `200`이면 정상이다.

**짧은 설명**

이 값이 너무 짧으면 정상적인 느린 요청까지 실패로 처리된다. 실제 서비스에서는 응답 시간을 먼저 측정한 뒤 여유를 두고 정해야 한다. 오늘은 "연결 거부와 응답 지연은 다른 종류의 장애이고, 대응 설정도 다르다"는 점만 확인한다.

## 다음 주차 예고

오늘 만든 구조에서는 `app1`을 멈춰도 서비스가 살아 있다. 이 성질을 이용하면 앱을 한 대씩 새 버전으로 교체해 **중단 없이 배포**할 수 있다. 로드밸런싱은 장애 대응 수단이면서 동시에 무중단 배포의 전제이기도 하다.

## 참고 자료

- [Nginx upstream 모듈](https://nginx.org/en/docs/http/ngx_http_upstream_module.html)
- [Nginx proxy_next_upstream](https://nginx.org/en/docs/http/ngx_http_proxy_module.html#proxy_next_upstream)
- [Nginx proxy 타임아웃 설정](https://nginx.org/en/docs/http/ngx_http_proxy_module.html#proxy_connect_timeout)
- [Docker Compose CLI 명령](https://docs.docker.com/reference/cli/docker/compose/)
