# 01. 환경 세팅 — 5주차 시작 전 확인

4주차와 **같은 PetClinic 프로젝트 루트**에서 진행합니다. 5주차는 새 프로그램을 설치하지 않고, 4주차에서 만든 `app` 서비스를 `app1`·`app2` 두 개로 나누는 시간입니다.

> Windows 사용자는 WSL2 Ubuntu 터미널에서, macOS 사용자는 기본 터미널에서 실행합니다.

새로 받을 이미지도 없습니다. 4주차에 쓰던 `spring-petclinic:dockerfile`과 `nginx:stable-alpine`을 그대로 사용합니다.

## 1. 모임 전 확인할 것

아래만 확인되면 시작할 수 있습니다.

```bash
docker version
docker compose version
curl --version
command -v grep awk seq
docker compose config
docker compose ps
```

| 확인 | 정상 기준 | 5주차에서 필요한 이유 |
| --- | --- | --- |
| Docker | `docker version`에 Client·Server 정보가 표시 | Nginx·app1·app2·PostgreSQL 실행 |
| Compose | 버전 표시 | 네 서비스를 한 구성으로 관리 |
| 기존 Compose | `config`, `ps`가 동작 | 4주차 구성을 이어서 사용 |
| curl | 버전 표시 | 반복 요청과 상태 코드 확인 |
| grep·seq | 각 명령의 경로 표시 | 로그에서 앱별 처리 횟수 세기 |

Docker Server 연결 오류가 나면 Docker Desktop을 실행합니다. WSL2 사용자는 **Settings → Resources → WSL Integration → Ubuntu**도 켭니다.

> `docker compose down -v`는 실행하지 않습니다. PostgreSQL Named Volume까지 지워 기존 데이터가 사라집니다.

## 2. 4주차 결과물이 남아 있는지 확인

5주차는 4주차의 최종 상태에서 시작합니다. 아래 다섯 가지가 있어야 합니다.

```bash
# 4주차에서 만든 Nginx 설정 파일 두 개가 있는지 확인한다.
ls -l nginx/conf.d/00-observability.conf nginx/conf.d/default.conf

# 4주차에서 만든 502 안내 화면이 있는지 확인한다.
ls -l nginx/html/study-50x.html

# 4주차 Compose에 app·nginx 서비스가 정의돼 있는지 확인한다.
grep -nE '^\s{2}(app|nginx|postgres):' docker-compose.yml

# 리버스 프록시 전달 주소가 app:8080으로 돼 있는지 확인한다.
grep -n 'proxy_pass' nginx/conf.d/default.conf

# PetClinic 이미지가 남아 있는지 확인한다.
docker image inspect spring-petclinic:dockerfile >/dev/null && echo 이미지있음
```

| 확인할 것 | 정상 기준 | 없으면 |
| --- | --- | --- |
| `00-observability.conf` | `log_format study`와 `proxy_cache_path`가 들어 있음 | 4주차 1-6·4-5-1을 다시 적용 |
| `default.conf` | `proxy_pass http://app:8080;`이 **두 군데**(정적·일반) 있음 | 4주차 4-5-2의 최종 내용으로 교체 |
| `study-50x.html` | 파일 존재 | 4주차 5-2를 다시 적용 |
| Compose 서비스 | `postgres`, `app`, `nginx` 세 개 | 4주차 1-2~1-4를 다시 적용 |
| PetClinic 이미지 | `이미지있음` 출력 | 아래 명령으로 다시 빌드 |

이미지가 없으면 다시 만듭니다.

```bash
./gradlew bootJar
docker build -t spring-petclinic:dockerfile .
```

> `proxy_pass`가 **두 군데** 있다는 점을 반드시 확인해 둡니다. 4주차 `default.conf`에는 정적 파일 `location`과 일반 `location /`에 각각 하나씩 있습니다. 5주차에는 **둘 다** 바꿔야 하며, 하나라도 `app:8080`으로 남아 있으면 Nginx가 시작하지 못합니다.

## 3. 지금 실행할 명령

수정 전 백업을 만들고 현재 상태를 확인합니다.

```bash
# 백업 파일 이름에 붙일 현재 날짜와 시각을 만든다.
STAMP=$(date +%Y%m%d-%H%M)

# 5주차 수정 전 상태로 Compose 파일과 Nginx 설정을 복사한다.
cp docker-compose.yml docker-compose.yml.before-week5-$STAMP
cp nginx/conf.d/default.conf nginx/conf.d/default.conf.before-week5-$STAMP

# 백업 파일이 실제로 만들어졌는지 확인한다.
ls -l docker-compose.yml.before-week5-* nginx/conf.d/default.conf.before-week5-*

# 4주차 구성이 실제로 동작하는지 한 번 실행해 확인한다.
docker compose up -d postgres app nginx
docker compose ps
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8080/
```

마지막 명령이 `200`이면 4주차 상태가 정상이며 5주차를 시작할 수 있습니다.

> `default.conf.before-week5-*` 백업 파일은 `nginx/conf.d/` 안에 만들어집니다. Nginx는 이 폴더의 `.conf`로 끝나는 파일만 읽으므로 백업 파일 이름이 `.conf`로 끝나지 않는 한 설정에 영향을 주지 않습니다.

확인이 끝나면 컨테이너를 내립니다. 데이터 볼륨은 유지합니다.

```bash
docker compose down
```

## 4. 5주차에서 사용할 구조 확인

### 바뀌는 부분만 보기

```text
4주차                              5주차
────────────────────────────────  ────────────────────────────────
사용자                             사용자
  ↓ localhost:8080                   ↓ localhost:8080
Nginx                              Nginx
  ↓ app:8080                         ├─ app1:8080
app                                  └─ app2:8080
  ↓ postgres:5432                          ↓ postgres:5432
PostgreSQL                         PostgreSQL
```

네트워크 구성은 4주차와 같습니다. 앱이 두 개가 되었을 뿐 연결 관계는 그대로입니다.

| 서비스 | 참여할 네트워크 | 이유 |
| --- | --- | --- |
| `nginx` | `proxy-net` | 사용자 요청을 받아 앱들에게 전달 |
| `app1` | `proxy-net`, `db-net` | Nginx 요청을 받고 DB에 접속 |
| `app2` | `proxy-net`, `db-net` | 위와 동일 |
| `postgres` | `db-net` | 두 앱의 DB 연결만 받음 |

### 포트

호스트에 공개하는 포트는 **여전히 `8080` 하나**입니다.

```text
브라우저 → 호스트 8080 → Nginx 컨테이너 80 ─┬→ app1 컨테이너 8080
                                            └→ app2 컨테이너 8080
```

`app1`·`app2`·`postgres`에는 `ports:`를 두지 않습니다. 앱이 두 개여도 사용자가 접속하는 주소는 바뀌지 않습니다.

### 포트 확인

시작 전에 호스트의 8080이 비어 있는지 확인합니다.

**Ubuntu / WSL2 Ubuntu**

```bash
sudo ss -ltnp '( sport = :8080 )'
```

**macOS**

```bash
sudo lsof -nP -iTCP:8080 -sTCP:LISTEN
```

Docker의 Nginx 외에 다른 프로세스가 8080을 쓰고 있으면 종료합니다. 로컬에서 `./gradlew bootRun`을 띄워 뒀다면 해당 터미널에서 `Ctrl+C`로 끕니다.

## 5. 앱별 로그 폴더 만들기

### 왜 폴더를 나누는가

4주차에는 앱이 하나여서 Tomcat access log도 한 곳에 저장했습니다.

```text
4주차:  app  → ./logs/tomcat        → 컨테이너의 /tmp/tomcat
```

5주차에 두 앱이 같은 폴더를 쓰면 **같은 날짜의 로그 파일 하나에 두 앱의 기록이 섞입니다.** 그러면 "어느 앱이 이 요청을 처리했는가"를 알 수 없습니다. 그래서 앱마다 다른 호스트 폴더를 연결합니다.

```text
5주차:  app1 → ./logs/tomcat/app1   → 컨테이너의 /tmp/tomcat
        app2 → ./logs/tomcat/app2   → 컨테이너의 /tmp/tomcat
```

컨테이너 안의 경로는 둘 다 `/tmp/tomcat`으로 같습니다. **호스트 쪽 폴더만 다릅니다.** 컨테이너는 서로를 모르므로 앱 설정은 완전히 동일해도 됩니다.

### 지금 만들 폴더

프로젝트 루트에서 실행합니다.

```bash
# app1·app2의 Tomcat 요청 로그가 각각 저장될 폴더를 만든다.
mkdir -p logs/tomcat/app1/access logs/tomcat/app2/access

# 두 폴더가 실제로 만들어졌는지 확인한다.
ls -ld logs/tomcat/app1/access logs/tomcat/app2/access
```

| 명령 | 하는 일 |
| --- | --- |
| `mkdir -p` | 상위 폴더가 없어도 함께 만들고, 이미 있어도 오류를 내지 않는다 |
| `logs/tomcat/app1/access` | `app1` 컨테이너의 `/tmp/tomcat/access`와 연결될 호스트 폴더 |
| `logs/tomcat/app2/access` | `app2` 컨테이너의 `/tmp/tomcat/access`와 연결될 호스트 폴더 |

4주차에 쓰던 `logs/tomcat/access` 폴더는 **지우지 않고 그대로 둡니다.** 5주차에는 사용하지 않을 뿐이며, 4주차 기록을 비교용으로 남겨 둘 수 있습니다.

### 이전 로그 정리 (선택)

앞선 실습 기록이 섞이면 개수 세기가 헷갈립니다. 4주차 로그를 따로 옮겨 두려면 다음을 실행합니다.

```bash
# 4주차 로그를 보관용 폴더로 옮긴다.
mkdir -p logs/tomcat/week4-archive
mv logs/tomcat/access/* logs/tomcat/week4-archive/ 2>/dev/null || true
```

지우는 것이 아니라 옮기는 것이므로 필요하면 다시 볼 수 있습니다.

## 6. 실습 중 문제가 생겼을 때 되돌리는 방법

5주차 실습은 Compose 파일과 `default.conf` 두 개만 수정합니다. 문제가 생기면 3절에서 만든 백업으로 되돌립니다.

```bash
# 백업 파일 목록을 확인한다.
ls -l docker-compose.yml.before-week5-* nginx/conf.d/default.conf.before-week5-*

# 4주차 상태로 되돌린다. 파일명의 시각 부분은 위 결과에 맞춰 바꾼다.
cp docker-compose.yml.before-week5-<시각> docker-compose.yml
cp nginx/conf.d/default.conf.before-week5-<시각> nginx/conf.d/default.conf

# 되돌린 설정으로 4주차 구성을 다시 실행한다.
docker compose up -d postgres app nginx
```

DB 볼륨은 어느 경우에도 삭제하지 않습니다.

## 완료 기준

- [ ] `docker compose config`, `docker compose ps`가 동작한다.
- [ ] 4주차의 `00-observability.conf`, `default.conf`, `study-50x.html`이 모두 있다.
- [ ] `default.conf`에 `proxy_pass http://app:8080;`이 **두 군데** 있는 것을 확인했다.
- [ ] `spring-petclinic:dockerfile` 이미지가 있다.
- [ ] 4주차 구성으로 `http://localhost:8080/`이 `200`을 반환하는 것을 확인했다.
- [ ] `docker-compose.yml`과 `default.conf`를 백업했다.
- [ ] 호스트 8080 포트가 비어 있다.
- [ ] `logs/tomcat/app1/access`, `logs/tomcat/app2/access` 폴더를 만들었다.
- [ ] 앱별로 로그 폴더를 나누는 이유를 이해했다.

## 참고

- [Docker Compose 공식 문서](https://docs.docker.com/compose/)
- [Docker Compose 볼륨 설정](https://docs.docker.com/reference/compose-file/services/#volumes)
- [Nginx 공식 Docker 이미지](https://hub.docker.com/_/nginx)
