# 02. 이론 — Nginx는 여러 앱에 요청을 어떻게 나누는가

## 목차

- 사전 지식. 4주차 구조에서 무엇만 달라지는가
- 0. 실습 공통: 어느 앱이 처리했는지 판정하는 방법
- 1. 앱 컨테이너를 두 개로 늘리기
- 2. 두 앱이 하나의 PostgreSQL을 함께 쓰기
- 3. Nginx upstream과 라운드 로빈
- 4. 장애 우회 설정
- 5. 모든 앱이 멈췄을 때
- 참고. 이 구조가 아직 해결하지 못한 것
- 참고 자료

## 사전 지식. 4주차 구조에서 무엇만 달라지는가

4주차에서 Nginx는 요청을 받아 **하나의 주소**로 전달했다.

```nginx
proxy_pass http://app:8080;
```

5주차에서는 전달할 수 있는 주소가 **여러 개**가 된다. 그래서 두 가지가 필요해진다.

1. 후보 주소들의 목록을 만드는 방법 → `upstream`
2. 그중 어디로 보낼지 정하는 규칙 → 라운드 로빈, 그리고 실패했을 때의 우회 규칙

| 4주차 | 5주차 |
| --- | --- |
| 전달 대상이 고정된 주소 하나 | 전달 대상이 이름이 붙은 목록 |
| 대상이 실패하면 곧바로 `502` | 대상이 실패하면 목록의 다른 대상을 시도 |
| Tomcat 로그 하나 | 앱마다 분리된 Tomcat 로그 |

캐시·요청 제한·보안 헤더·오류 안내 화면은 4주차 설정을 그대로 유지한다. 이번 주차에 새로 배우는 것은 **`upstream`과 장애 우회** 두 가지다.

## 0. 실습 공통: 어느 앱이 처리했는지 판정하는 방법

이번 실습의 거의 모든 판정은 "이 요청이 어느 앱까지 갔는가"로 이루어진다. 확인할 근거는 세 가지다.

| 근거 | 확인 대상 | 알 수 있는 것 |
| --- | --- | --- |
| `curl`의 상태 코드 | 사용자가 받은 결과 | 서비스가 살아 있는지 |
| Nginx access log의 `upstream` | Nginx가 실제로 보낸 주소 | 어느 앱에 전달했는지, 재시도했는지 |
| 앱별 Tomcat access log | 각 앱이 받은 요청 | 그 앱까지 실제로 도달했는지 |

### 0-1. `upstream` 필드는 이름이 아니라 IP로 기록된다

4주차 로그 형식에는 이미 `upstream="$upstream_addr"`이 들어 있다. 그런데 이 값은 `app1`이 아니라 `172.18.0.4:8080` 같은 **IP 주소**로 기록된다. Nginx는 설정을 읽는 시점에 이름을 IP로 바꿔 두기 때문이다.

따라서 로그의 IP가 어느 컨테이너인지 알려면 대응표를 따로 확인해야 한다. 실습에서는 `docker inspect`로 각 앱의 IP를 조회해 대응시킨다.

### 0-2. 재시도가 있으면 `upstream`에 주소가 두 개 기록된다

한 요청이 `app1`에서 실패해 `app2`로 넘어가면 로그는 다음처럼 남는다.

```text
upstream="172.18.0.4:8080, 172.18.0.5:8080"
```

쉼표로 이어진 값은 **한 요청이 두 대상에 순서대로 시도됐다**는 뜻이다. 이 표시가 장애 우회가 실제로 동작했다는 가장 직접적인 증거다.

### 0-3. Tomcat 로그는 앱마다 다른 폴더에 쌓인다

```text
호스트 ./logs/tomcat/app1  ─ 연결 ─  app1 컨테이너의 /tmp/tomcat
호스트 ./logs/tomcat/app2  ─ 연결 ─  app2 컨테이너의 /tmp/tomcat
```

컨테이너 안의 경로는 동일하고 호스트 폴더만 다르다. 컨테이너는 자기 밖을 모르므로 두 앱의 환경 변수는 완전히 같아도 된다.

요청마다 `?study=week5-...` 표식을 붙이고 두 폴더에서 각각 개수를 세면, 어느 앱이 몇 건을 처리했는지 알 수 있다.

### 0-4. 판정 조합

| Nginx 로그 | app1 로그 | app2 로그 | 해석 |
| --- | --- | --- | --- |
| `status=200` | 있음 | 없음 | app1이 처리 |
| `status=200` | 없음 | 있음 | app2가 처리 |
| `status=200`, `upstream`에 주소 2개 | 없음 | 있음 | app1 실패 → app2로 우회 성공 |
| `status=502` | 없음 | 없음 | 전달할 앱이 없음 |
| `cache=HIT` | 없음 | 없음 | Nginx 캐시가 응답. 로드밸런싱과 무관 |

마지막 줄이 중요하다. 정적 파일 요청은 캐시에서 끝나 앱까지 가지 않으므로 **분산 확인에 쓰면 안 된다.** 실습에서는 캐시하지 않는 `/owners` 같은 동적 경로를 사용한다.

## 1. 앱 컨테이너를 두 개로 늘리기

### 1-1. Compose에서 바뀌는 위치

| 파일과 위치 | 5주차에서 하는 변경 |
| --- | --- |
| `docker-compose.yml`의 `services.app` | 서비스 이름을 `app1`로 바꾸고, 로그 볼륨 경로를 `./logs/tomcat/app1`로 바꾼다 |
| `docker-compose.yml`의 `services.app2` | `app1` 블록을 복사해 새로 추가한다. 로그 볼륨만 `./logs/tomcat/app2`로 바꾼다 |
| `docker-compose.yml`의 `services.nginx.depends_on` | `[app]`을 `[app1, app2]`로 바꾼다 |
| `nginx/conf.d/default.conf` 최상단 | `upstream petclinic_backend` 블록을 추가한다 |
| `default.conf`의 두 `location` | `proxy_pass`를 `http://petclinic_backend`로 바꾸고 장애 우회 설정을 넣는다 |
| `services.postgres` | 바꾸지 않는다 |

`app1`과 `app2`의 차이는 **이름과 로그 볼륨 경로뿐**이다. 이미지·환경 변수·네트워크·`depends_on`은 완전히 동일하다.

### 1-2. 왜 `--scale`을 쓰지 않는가

Compose에는 같은 서비스를 여러 개 띄우는 기능이 있다.

```bash
docker compose up -d --scale app=2
```

이 방법이 더 간단하지만 이번 실습에는 맞지 않는다.

| 항목 | `--scale` | 서비스 두 개로 분리 |
| --- | --- | --- |
| 컨테이너 이름 | 자동 생성 (`프로젝트-app-1`, `-2`) | `app1`, `app2`로 고정 |
| 볼륨 | 두 컨테이너가 **같은 호스트 폴더** 사용 | 앱별로 다른 폴더 사용 |
| 로그 구분 | 한 파일에 섞여 구분 불가 | 폴더로 완전히 분리 |
| `stop` 대상 지정 | 개별 지정이 번거로움 | `stop app1`로 바로 지정 |

이번 주차의 목적은 "어느 앱이 처리했는가"를 눈으로 확인하는 것이므로 서비스를 명시적으로 나눈다.

### 1-3. 두 앱 모두 호스트 포트를 열지 않는다

```yaml
expose: ["8080"]
```

`expose`는 컨테이너 내부에서 8080을 쓴다는 표시일 뿐 호스트에 포트를 열지 않는다. 따라서 사용자가 접속할 수 있는 주소는 여전히 Nginx의 `localhost:8080` 하나다.

만약 두 앱에 `ports:`를 붙이면 어떻게 될까. 첫째, 두 앱이 같은 호스트 포트를 쓸 수 없어 서로 다른 포트를 줘야 한다. 둘째, 사용자가 로드밸런서를 건너뛰고 특정 앱에 직접 접속할 수 있게 되어 구조가 무너진다. 그래서 앱에는 포트를 열지 않는다.

## 2. 두 앱이 하나의 PostgreSQL을 함께 쓰기

### 2-1. 왜 DB는 늘리지 않는가

앱은 요청을 처리할 뿐 데이터를 갖고 있지 않다. 데이터는 DB에 있다. 그래서 앱은 쉽게 늘릴 수 있지만 DB는 그렇지 않다.

```text
app1 ─┐
      ├─ postgres:5432   ← 데이터의 단일 출처
app2 ─┘
```

두 앱이 같은 DB를 보기 때문에, `app1`에서 등록한 소유자를 `app2`가 처리한 요청에서도 조회할 수 있다. 이것이 사용자가 어느 앱에 배정되든 같은 결과를 보는 이유다.

DB를 여러 대로 늘리는 것은 복제·정합성 문제가 함께 따라오는 별개의 주제다. 이번 주차 범위가 아니다.

### 2-2. 초기화 SQL 재실행 방지

4주차에서 넣은 다음 설정을 두 앱 모두에 그대로 유지한다.

```yaml
SPRING_SQL_INIT_MODE: never
```

앱이 시작할 때 테이블 생성 SQL을 실행하지 않게 하는 설정이다. 이미 테이블이 있는 DB에 두 앱이 동시에 시작하면서 각자 테이블을 만들려 하면 `already exists` 오류로 앱이 죽는다.

이 설정은 **DB 동시 접근을 막는 것이 아니다.** DB는 원래 여러 클라이언트의 동시 접속을 전제로 만들어졌다. 여기서 막는 것은 시작 시 초기화 작업의 중복뿐이다.

### 2-3. 커넥션 풀

앱이 두 대가 되면 DB에 연결되는 커넥션 수도 두 배가 된다. Spring Boot 기본값은 앱당 최대 10개이므로 이번 구성에서는 최대 20개다. PostgreSQL 기본 허용치는 100개이므로 문제가 없다.

앱을 수십 대로 늘리는 상황이라면 이 값이 실제 제약이 되고, 그때는 커넥션 풀 크기 조정이나 별도의 커넥션 풀러가 필요해진다. 오늘은 "앱을 늘리면 DB 쪽 부담도 함께 늘어난다"는 사실만 알아 둔다.

## 3. Nginx upstream과 라운드 로빈

### 3-1. upstream 블록

```nginx
upstream petclinic_backend {
  server app1:8080 max_fails=1 fail_timeout=10s;
  server app2:8080 max_fails=1 fail_timeout=10s;
}
```

| 구성 | 의미 |
| --- | --- |
| `upstream petclinic_backend` | 앱 서버 묶음에 `petclinic_backend`라는 이름을 붙인다 |
| `server app1:8080` | 묶음에 앱 하나를 등록한다. `app1`은 Compose 서비스명이다 |
| `max_fails=1` | 연결 실패가 1회 발생하면 그 앱을 제외 대상으로 표시한다 |
| `fail_timeout=10s` | 두 가지를 뜻한다. 실패를 세는 기간이 10초이고, 제외된 앱을 다시 시도하지 않는 기간도 10초다 |

`upstream`은 `http` 범위에 써야 한다. `conf.d/*.conf`의 내용은 Nginx 기본 설정의 `http {}` 안에 포함되므로, `default.conf`에서 `server {}` **바깥**, 파일 맨 위에 두면 된다.

`max_fails=1`, `fail_timeout=10s`는 실습에서 결과를 빨리 보기 위한 값이다. 실제 운영에서는 일시적인 오류로 앱이 너무 쉽게 제외되지 않도록 더 큰 값을 쓴다.

### 3-2. proxy_pass 대상 변경

```nginx
# 4주차
proxy_pass http://app:8080;

# 5주차
proxy_pass http://petclinic_backend;
```

`petclinic_backend`는 호스트 이름이 아니라 위에서 정의한 upstream 이름이다. Nginx는 이 이름을 보고 등록된 서버 중 하나를 고른다.

> **주의:** 4주차 `default.conf`에는 `proxy_pass`가 **두 군데** 있다. 정적 파일 `location`과 일반 `location /`이다. 하나만 바꾸면 나머지 하나가 존재하지 않는 `app`을 가리키게 되고, Nginx는 `host not found in upstream "app"` 오류로 아예 시작하지 못한다.

### 3-3. 라운드 로빈

`upstream`에 아무 방식도 지정하지 않으면 기본값은 라운드 로빈이다. 등록된 순서대로 돌아가며 요청을 보낸다.

```text
요청 1 → app1        요청 4 → app2
요청 2 → app2        요청 5 → app1
요청 3 → app1        요청 6 → app2
```

요청 10건을 보내면 5건씩 나뉘는 것이 기본이다. 다만 다음 경우에는 정확히 반반이 아닐 수 있다.

| 원인 | 결과 |
| --- | --- |
| 정적 파일 요청 사용 | 캐시 `HIT`이면 앱까지 가지 않아 개수가 줄어든다 |
| 앱 하나가 실패로 제외됨 | 제외 기간에는 한쪽으로만 간다 |
| 브라우저의 부가 요청 | 화면 접속 시 CSS·이미지 요청이 섞여 들어온다 |

그래서 실습에서는 브라우저가 아니라 `curl`로, 그리고 캐시하지 않는 동적 경로로 확인한다.

### 3-4. 다른 분산 방식

| 지시어 | 기준 | 쓰는 상황 |
| --- | --- | --- |
| (기본) | 순서대로 | 서버 성능이 비슷할 때 |
| `weight=3` | 지정한 비율 | 서버 성능이 다를 때 |
| `least_conn;` | 현재 연결이 가장 적은 서버 | 요청별 처리 시간 편차가 클 때 |
| `ip_hash;` | 클라이언트 IP의 해시 | 같은 사용자를 같은 서버에 묶어야 할 때 |

`ip_hash`는 세션을 앱이 갖고 있는 서비스에서 임시로 쓰는 방법이다. 다만 특정 IP에 트래픽이 몰리면 분산이 깨지므로, 근본적으로는 세션을 DB나 Redis 같은 공용 저장소로 옮기는 편이 낫다. PetClinic은 로그인 기능이 없어 이번 실습에서는 이 문제가 드러나지 않는다.

## 4. 장애 우회 설정

### 4-1. 두 가지 설정의 역할 구분

```nginx
# upstream 안 — 앱 단위
server app1:8080 max_fails=1 fail_timeout=10s;

# location 안 — 요청 단위
proxy_next_upstream error timeout http_502 http_503 http_504;
proxy_next_upstream_tries 2;
```

두 설정은 시간 축이 다르다.

```text
proxy_next_upstream     : 지금 이 요청 하나를 어떻게 구제할 것인가
max_fails/fail_timeout  : 앞으로 이 앱을 얼마 동안 건너뛸 것인가
```

`proxy_next_upstream`만 있고 `max_fails`가 없으면, 매 요청마다 죽은 앱에 먼저 시도했다가 실패하고 우회하는 일이 반복된다. `max_fails`가 있으면 한 번 실패한 뒤로는 아예 시도하지 않아 지연이 사라진다.

### 4-2. proxy_next_upstream의 조건

| 조건 | 언제 다시 시도하는가 |
| --- | --- |
| `error` | 연결·전송·읽기 중 오류가 발생했을 때 |
| `timeout` | 연결·전송·읽기가 제한 시간을 넘겼을 때 |
| `http_502` `http_503` `http_504` | 앱이 이 상태 코드를 반환했을 때 |

`http_500`이나 `http_404`는 넣지 않는다. 이건 앱이 정상적으로 살아서 판단한 결과이므로, 다른 앱에 보내도 같은 결과가 나온다. 재시도해 봐야 의미가 없고 오히려 같은 요청이 두 번 처리되는 문제가 생길 수 있다.

`proxy_next_upstream_tries 2`는 한 요청이 최대 2개 앱까지만 시도하게 한다. 이 값이 없으면 앱이 많을 때 하나의 요청이 모든 앱을 순회하며 응답이 크게 늦어질 수 있다.

### 4-3. 재시도할 수 없는 경우

Nginx가 이미 클라이언트에게 응답 본문을 보내기 시작했다면 되돌릴 수 없다. 앞부분을 보낸 뒤 다른 앱의 응답으로 바꿀 수는 없기 때문이다.

```text
연결 실패 → 아직 아무것도 안 보냄 → 재시도 가능
응답 전송 중 끊김 → 이미 일부 전송됨 → 재시도 불가
```

그래서 `proxy_next_upstream`은 **응답 전송이 시작되기 전의 실패**에만 적용된다.

### 4-4. 능동 헬스체크가 아니다

이 방식은 Nginx가 앱 상태를 미리 검사하지 않는다. 실제 사용자 요청을 보내다가 실패했을 때 알아채는 **수동 감지**다.

| 방식 | 동작 | 지원 |
| --- | --- | --- |
| 수동 감지 (오늘 사용) | 요청 전달 중 실패를 감지해 우회 | 오픈소스 Nginx |
| 능동 헬스체크 | 주기적으로 앱에 검사 요청을 보내 미리 상태 파악 | NGINX Plus (상용) |

수동 감지의 한계는 **실패를 알아채기 위해 실제 요청 하나가 희생된다**는 점이다. 그 요청은 우회 덕분에 사용자에게는 성공으로 보이지만, 응답 시간은 조금 길어진다.

### 4-5. 앱이 죽지 않고 느려지기만 한다면

`docker compose stop`으로 컨테이너를 멈추면 연결 자체가 즉시 거부된다(`Connection refused`). 실패를 곧바로 알 수 있어 우회도 빠르다.

그런데 실제 장애는 앱이 살아 있으면서 응답만 하지 않는 경우가 더 많다. 이때는 연결이 되므로 오류가 나지 않고, Nginx는 기본값인 60초까지 기다린다. 이런 상황에서 우회가 동작하려면 기다리는 한계를 정해야 한다.

```nginx
proxy_connect_timeout 2s;
proxy_read_timeout 10s;
```

이 값이 너무 짧으면 정상적인 느린 요청까지 실패로 처리되므로 실제 응답 시간을 측정한 뒤 정해야 한다. 실습에서는 선택 단계로 다룬다.

## 5. 모든 앱이 멈췄을 때

### 5-1. 여전히 502다

앱이 두 대여도 둘 다 멈추면 전달할 곳이 없다.

```text
사용자 → Nginx: 연결 성공
Nginx → app1: 연결 실패
Nginx → app2: 연결 실패
사용자 ← Nginx: 502 Bad Gateway
```

Nginx error log에는 다음 중 하나가 나타난다.

| 메시지 | 뜻 |
| --- | --- |
| `connect() failed (111: Connection refused)` | 해당 앱에 연결을 시도했으나 거부됨 |
| `no live upstreams while connecting to upstream` | 모든 앱이 제외 상태여서 시도할 대상이 없음 |

두 번째 메시지는 앱이 두 대 이상일 때만 나타나는 표현이다. `max_fails`로 두 앱이 모두 제외 표시된 뒤에 들어온 요청에서 볼 수 있다.

### 5-2. 4주차 안내 화면은 그대로 동작한다

4주차에 설정한 다음 내용은 수정하지 않는다.

```nginx
error_page 502 503 504 /study-50x.html;
location = /study-50x.html { root /usr/share/nginx/html; internal; }
```

앱이 몇 대든 최종적으로 502가 만들어지면 같은 안내 화면이 나온다. 여기서 확인할 것은 **화면이 바뀌어도 HTTP 상태는 `502`로 유지되는가**이다. 상태를 `200`으로 바꾸면 모니터링 도구가 장애를 정상으로 오해한다.

### 5-3. 앱이 멈춘 상태에서는 Nginx를 reload하지 않는다

주의할 동작이 하나 있다. Nginx는 설정을 읽는 시점에 `app1`·`app2`라는 이름을 IP로 변환한다. 앱이 멈춰 있으면 그 이름을 찾을 수 없다.

```text
앱이 멈춘 상태에서 nginx -s reload
→ host not found in upstream "app1"
→ 설정 반영 실패
```

따라서 설정 변경은 앱이 실행 중일 때 한다. 장애 실습 중에 설정을 고쳐야 한다면 앱을 먼저 다시 시작한다.

## 참고: 이 구조가 아직 해결하지 못한 것

앱 계층의 단일 장애점은 없앴지만 다른 계층은 그대로다.

| 계층 | 현재 상태 | 실제 운영에서의 접근 |
| --- | --- | --- |
| Nginx | 한 대. 멈추면 전체 중단 | 로드밸런서 이중화, DNS 라운드 로빈, 클라우드 관리형 LB |
| 앱 | **두 대. 오늘 해결** | 오토스케일링으로 부하에 따라 자동 증감 |
| PostgreSQL | 한 대. 멈추면 두 앱 모두 동작 불가 | 복제(replication), 읽기 전용 복제본 분리 |
| 배포 | 앱을 멈췄다 켜는 동안 중단 | 한 대씩 교체하는 롤링 업데이트 |

마지막 줄이 흥미로운 지점이다. 오늘 만든 구조에서는 `app1`을 멈춰도 서비스가 살아 있으므로, **한 대씩 새 버전으로 교체하면 중단 없이 배포할 수 있다.** 로드밸런싱은 장애 대응만이 아니라 무중단 배포의 전제이기도 하다.

## 참고 자료

- [Nginx upstream 모듈](https://nginx.org/en/docs/http/ngx_http_upstream_module.html)
- [Nginx proxy_next_upstream](https://nginx.org/en/docs/http/ngx_http_proxy_module.html#proxy_next_upstream)
- [Nginx 로드밸런싱 안내 문서](https://docs.nginx.com/nginx/admin-guide/load-balancer/http-load-balancer/)
- [Docker Compose 서비스 정의](https://docs.docker.com/reference/compose-file/services/)
- 『이것이 리눅스다』 20.2 도커의 개념과 실습, 4.6 프로세스·서비스, 4.7 서비스와 소켓
